# USB-C adapter census — both gates (a2p25 RBR-x4 qualification)

Two independent gates decide whether an adapter can deliver 1080p60 from an
RBR-only source (the a2p25 D-PHY-as-SerDes plan needs 4 lanes at RBR):

1. **PD/mux gate** — DP alt-mode VDO pin assignments from Discover Modes
   (`telnet 'd'`, ESP32 census, e3c41cbe). C/E = the mux path carries 4
   lanes; D/F = 2 lanes + USB3.
2. **DPCD gate** — the sink/converter chip's OWN `MAX_LINK_RATE` (0x001) and
   `MAX_LANE_COUNT` (0x002), invisible to PD. Captured by the AUX engine's
   attach-time caps read (68ec8bab): dp_test telemetry line D6 `O:rrll`
   (rr = 0x001 raw, ll = 0x002 raw: low nibble lanes, bit7 enhanced
   framing, bit6 TPS3); full core: OSPI regs 0x3C/0x3D, `d` prints
   `sink caps:`.

Verdict for RBR-x4 = BOTH gates must show 4 lanes. A 2-lane DPCD cap means
2 x 1.62 Gbps = 3.24 Gbps, short of 1080p60's ~4.46 Gbps -> 720p ceiling.

Method: 60K card, dp_test bitstream 232c81f1 (colorbars + telemetry),
census firmware d7b34197, readout over the WiFi telnet tee (USB-C is the
DP port, so serial is unavailable during the test).

| Date | Adapter | PD VDO | PD pins / verdict | DPCD O: | Rate max | Lanes max | RBR-x4 verdict |
|------|---------|--------|-------------------|---------|----------|-----------|----------------|
| 2026-09-02 | Anker hub (IT6563 DP->HDMI) | 00000C05 | C+D / "4-LANE OK" | 14C2 | HBR2 | **2** (+ef +tps3) | **FAIL — 720p ceiling** (PD gate alone would have passed it) |
| 2026-09-02 | Mindorlan USB-C hub/adapter | 00000C05 | C+D / "4-LANE OK" | 14C2 | HBR2 | **2** (+ef +tps3) | **FAIL — 720p ceiling** (identical caps to the Anker; EDID given up after 41 DEFERs, `U:69`) |
| 2026-09-02 | Ugreen hub (the strict-converter/overdrive-class unit) | 00000C05 | C+D / "4-LANE OK" | 14C2 | HBR2 | **2** (+ef +tps3) | **FAIL — 720p ceiling** (third identical reading; trained clean at 804 mV, `G:F1 U:08`) |
| 2026-09-02 | Generic USB-C->HDMI 4K dongle with 100 W PD passthrough (charger on its PD-in sourced VBUS -> card negotiated as power SNK) | 00000C05 | C+D / "4-LANE OK" | 0000 (never read) | ? | ? | **UNMEASURED — AUX dead**: PD/mux/HPD all good (`DP-ACTIVE`, `HPD=1`, `MUX=1`, FPGA `HLVC:1000`) but the ladder sits at `check_presence` with ZERO AUX reply edges (`E:00`) in BOTH flip polarities and after an HPD-pulse retrain; timeouts saturate (`T:000F`); no training, no colorbars. Sink DPCD unreadable -> separate AUX-electrical investigation (candidate: spec AUX DC-bias presence detect vs our pulldown-both pad config). Physical plug flip: no change. Device CONFIRMED GOOD on Mac and iPhone (HDMI out on both) -> a2mega-side. CLASSIFIED: the documented 1.0a3 AUX-RX limitation (aux-rx findings / board_1_0a4_requirements.md): AC-coupled AUX with no FPGA-side DC bias -> TLVDS receiver stores an offset and decodes converter/hub-class replies only; monitor/dongle-class sinks are offset-blind (E:00). **Second gate is unmeasurable for this sink class on 1.0a3 until the 1.0a4 receiver-bias divider.** Blind ladder (BLIND_SINK=1) would still drive video through it open-loop. NOTE: its 4K claim implies an HBR2 x4-class converter — the very 4-lane sink the RBR-x4 plan wants to find, and the class this board rev is blind to |
| 2026-09-02 | Cable Matters 48 Gbps unidirectional USB-C->HDMI 2.1 adapter — **Synaptics VMM7100** single-chip DP1.4->HDMI 2.1 converter w/ integrated PD controller (slot-powered card; card sources VBUS, adapter LED lit) | — (no Discover Modes) | — | — | ? | ? | **UNMEASURED — PD-SILENT**: FUSB302 attach OK on CC1 (SRC/DFP, VCONN sourced on Ra pin), one hard reset at attach, then parked in `SOURCE-WAIT-REQUEST`: our 5 V/1 A Source_Caps (150 ms cadence) never draw a Request; no alt-mode entry, `MUX=0 HPD=0`; no new PD events over 25 s (not a VBUS-collapse livelock). Both gates blocked at the PD layer. LED steady. TRACE (fw 3118ade7+): NOT the BolAAzuL loop — the VMM7100 GoodCRCs our Source_Caps then sends HARD RESET 30 ms later; after our VBUS-cycle recovery the next Source_Caps produced NO TX interrupt ever (FUSB302 PD engine wedged; ring frozen with tx_busy=1 — the real stuck-tx_busy case). Fix 43b478f1: FUSB302 PD_RESET + FIFO flush after any Hard Reset + 250 ms TX watchdog -> contract established, full discovery (PINS=C 4-LANE OK). Then exposed an ACK-BEFORE-TXOK RACE in our stack (its ~1 ms ACKs beat our TX_SUCCESS poll; next VDM refused; 45 ms retry of the previous Discover answered CRC-bad -> USB-only). Fix 30fd4f47: a VDM response completes the pending TX. RESULT on 30fd4f47: PD GATE PASS — Modes/Enter/Status/Configure ACKs at +52/+55/+59/+62 ms, `DP-ACTIVE` at +64 ms, `MUX=1 HPD=1`, HPD Attention at +1.04 s. FPGA: HPD seen but the AUX receiver decodes exactly ONE byte per reply (the ACK header; `E:` +0x11 per ceremony, `R:0`) -> every presence read is a short reply -> ladder `error`/restart (`D:00`), no training, `O:0000`. = the 1.0a3 AUX-RX offset-marginal class ("one ACK then blind"), so the DPCD gate is UNMEASURABLE on this board rev (1.0a4 receiver bias). Likely HBR3 x4 by chip spec. Known-good on Mac: CONFIRMED |
| 2026-09-02 | **Sceptre DP monitor via Cable Matters USB-C->DP cable** — slot-powered card, fw 79538724 (pin-E fix) | 00001005 (plug) | E / "4-LANE OK" | **14C4** | HBR2 | **4** (+ef +tps3) | **PASS — RBR-x4 viable.** Both gates 4-lane. Closed-loop link trained HBR x2 (`C:0177`), streaming (`K:03`), SE 0/0, EDID answered (`U:00`), video live. ⚠ BEFORE fw 79538724 this cable was refused by our PD stack (`no DP sink mode with pin C` -> USB-only): plugs offer pin E, and a2mega had never negotiated E |
| 2026-09-02 | Fangor USB-C portable monitor (USB-C input; monitor sources VBUS -> card SNK; first CC2-orientation device) | 00000405 | C / "4-LANE OK" | 0000 (never read) | ? | ? | **UNMEASURED — AUX offset-blind**: PD `DP-ACTIVE`, `MUX=1 HPD=1`, mux regs consistent for CC2 (`0x0A=1E`, `0x13=A0`), FPGA sees HPD, but ZERO AUX reply edges (`E:00`) in both crossbar states (`f`) and after retrain. Physical plug flip: no change. KNOWN sink: this monitor has NEVER lit via alt-mode on 1.0a3 except under BLIND_SINK — it is the campaign's blind-ladder monitor. Lane telemetry is therefore unobtainable on this board rev by construction (blind mode reads no replies); only its PD gate (pin C, 4-lane mux path) is known. 1.0a4 receiver bias. (One `DP ALT MODE RESPONSE TIMEOUT` during PD, recovered.) |
| 2026-09-02 | **BolAAzuL USB-C->DisplayPort multi-function adapter dongle** (active; us-powered in a slot) | 00000405 | C / "4-LANE OK" | **14C4** | HBR2 | **4** (+ef +tps3) | **PASS — RBR-x4 viable** (fw be2ab629). Closed-loop trained HBR x2 (`C:0177`), streaming (`K:03`), SE 0/0, video live, AUX readable (`E:DC`). Needed THREE fw fixes, found with the PD event trace (`t`): (1) our RDO rule also enforced MAX current -> we REJECTED its Capability-Mismatch Request -> it hard-reset -> ~1 s loop forever (looked "PD-silent"); (2) it never ACKs DP_CONFIGURE and reports via Attention ~1.8 s after the LAST Configure it saw -> our 45 ms x 3 (and 500 ms x 7) resends kept restarting its bring-up; now Configure is sent once, 3 s quiet, one retry, and an Attention before the ACK promotes to DP_ACTIVE; (3) Enter/Status get 500 ms x 7 patience. All benign for compliant adapters (they ACK in ms). Mac also takes a few seconds to bring it up. PICTURE DELAY after D4 (video-live LED): `Y:33 T:FF21 Z:0000664F` = two gate-fail teardowns/retrains after first establish — sink reported 0x202=0x66 (CR_DONE clear, EQ+symbol set), 0x204=00 at >=1.5 s post-establish, the Anker-class post-establish settling that GATE_GRACE (OFF in dp_test, round-1 verdict) exists for; picture appears once the link stays up. SECOND hot-plug (warm): colorbars INSTANT, `Y:11 T:0002 B:0000000` = clean first-try establish, zero teardowns -> the delay was converter cold-start settling, not systematic. Second persona (card bus-powered BY the adapter, no slot power): contract OK but its DR_Swap goes unanswered -> UFP, no DP (bench-only) |

Board-rev caveat: on 1.0a3 the DPCD gate is readable only from sinks whose AUX replies the offset-marginal receiver can decode. Hub/converter class reads; the generic 4K dongle did NOT (E:00); the DP monitor DID read cleanly (E:18-EC, O:14C4), so "monitor-class = blind" from the earlier bring-up rounds is not universal — it is per-sink. Offset-blind sinks read O:0000 with E:00 regardless of their real caps — that is the 1.0a3 AUX-RX limitation, not a census result. The 1.0a4 receiver-bias fix lifts it.

Result (n=8 devices): TWO 4-lane sinks at both gates — the Sceptre DP monitor over a direct USB-C->DP cable and the BolAAzuL USB-C->DP dongle (both PASS, both needed PD-stack fixes to negotiate at all). Every hub-class adapter is a 2-lane HBR2 converter behind a 4-lane PD advertisement (720p ceiling for RBR-only). Three devices are unmeasured on this board rev: AUX-offset-blind/marginal on 1.0a3 (generic 4K dongle E:00, Fangor E:00, Cable Matters VMM7100 header-only) and the Cable Matters VMM7100 now passes the PD gate (after four PD-stack fixes) but is AUX offset-marginal on 1.0a3 (header-only decode) -> DPCD gate unmeasurable, same class as the Fangor/dongle. Readability is per-sink: the direct DP monitor and all hubs read; the Fangor and the dongle do not. Side finding with product impact: our PD stack rejected pin assignment E, so direct USB-C->DP cables never worked on a2mega until fw 79538724.

Emerging pattern (n=3): every hub-class adapter so far advertises C/D pin
assignments (4-lane mux path) yet carries a 2-lane HBR2 DP->HDMI converter
(IT6563 class). For an RBR-only source that is a 720p ceiling on hubs
regardless of the mux; the open question is a DIRECT USB-C->DP path to a
real DP monitor, where the monitor's own DPCD is the sink.

Regression on fw be2ab629 (RDO rule + Configure-once/Attention promotion + Enter/Status patience): ANKER re-plugged — identical negotiation and telemetry to the pre-fix run (PINS=CD, Enter/Status/Configure ACKs at +12/+22/+28 ms, DP_ACTIVE at +30 ms, HBR x2, streaming, SE 0/0, O:14C2, Y:11, T:000C U:08). The fixes are invisible to a compliant adapter; they only change behavior where the old code failed. Re-run on the FINAL build 30fd4f47 (adds PD_RESET after Hard Reset, TX watchdog, ACK-before-TXOK race fix): Anker `PINS=CD O:14C2` HBR x2 streaming SE 0/0, Ugreen colorbars — PASS. SHIPPABILITY (ed, 2026-09-02): approved as default behavior — no breakage observed across the full pile.

Blind-sink test build (ed, 2026-09-02: "keep a separate blind sink build around for testing")
- Project `boards/a2mega/a2mega_dp_test_blind.gprj` -> `a2mega_dp_test_blind_top` (thin wrapper: `a2mega_dp_test_top #(.BLIND(1))`), same pins/CST; SDC is GENERATED (`hdl/dp_test/gen_blind_sdc.sh` prefixes `i_core/`) — re-run it whenever `a2mega_dp_test.sdc` changes.
- Build: `GPRJ=a2mega_dp_test_blind.gprj tools/build.sh a2mega`; flash: `GPRJ=a2mega_dp_test_blind.gprj tools/flash.sh a2mega`. First build 39f0a8e5: 23 setup viols (clk100 in i_dp worst -0.649, freq_ok instrument -1.485) — the timing-stale dp_test netlist in a worse draw; needs the same dp_test timing pass. NOT flashed yet.
- Use: lights sinks the 1.0a3 AUX receiver cannot decode (Fangor, generic 4K dongle, VMM7100) open-loop; cannot read DPCD caps (O: stays 0000 by construction).

Notes
- Anker link health at capture: HBR x2 trained (`C:0177`), sink streaming
  (`K:03`), symbol-error counters valid and zero (`SE0/SE1:8000`).
- The legacy lossy decode (`X:91`) read the Anker's HBR2 sink as "no
  supported rates" (it only recognizes 0x06/0x0A) — the raw tap is
  required for this census.
