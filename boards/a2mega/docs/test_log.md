# a2mega DP test log

Structured record of every hardware test. **Protocol: no test result is
reported or acted on unless it has a row here.** The bringup log
(`bringup_1_0a3_log.md`) stays the narrative journal; this file is the
scannable ground truth.

## Column key

- **#**: test id (monotonic)
- **GW**: FPGA bitstream — sha256 prefix of the flashed `.fs` (git-committed)
- **FW**: ESP32 firmware — commit hash it was built from
- **LEDs** (dp_test builds; silkscreen D1-D4, all active-low; user-verified
  08-18): D1/D15=led0 heartbeat (flashing = configured). D2/D14=led1 HPD
  (lit = attach+orientation+cable good; seldom dark). D3/R14=led2 freq_ok
  135MHz (solid = bitstream running; lags D4 ~0.3-0.5s = counter window
  latency, NOT clock latency; header comment in top.sv saying
  'link_established' is STALE). D4/P14=led3 video_live — THE draw verdict,
  delivered instantly: good draw = solid right away (training completes
  <1s, before D3's window fills); bad draw = flashing (link-flap mode) or
  dark (never-trains mode). No trains-after-retries mode exists at boot;
  D4 at power-on is a complete instant classifier.
- **Board** (carrier+SOM pairs — SOMs migrated during the flash saga):
  `B1` = carrier #1 (original) + SOM #2, in service from Sat 08-16 9am
  (all Sat hub successes incl. first light 15:20 / 39ada3ca);
  `pre-switch` = carrier #1 + SOM #1 (through Fri + Sat early AM);
  `B2` = carrier #2 (fresh) + SOM #1 (revived), in service 08-17
  (all 08-17 rows; 0/12+ hub draws).
- **Sink**: `mon` (USB-C monitor direct) / `hubA` (Anker A8365 → HDMI) /
  other (add to roster below)
- **Cable**: id from the cable roster below
- **Orient**: plug orientation as *detected* (`CC1`/`CC2` from PD telemetry),
  or `?` if not captured. "logo-up" style notes go in Notes.
- **Brk**: USB-C breakout inline? `Y`/`N` — **load-bearing variable** (AUX
  attenuation), never omit
- **Pwr**: board power source — `mon` (monitor VBUS, replugs = power cycle)
  / `slot` (IIgs) / `mac` (USB bench)
- **EQ**: mux DP RX EQ in dB at test time (fw default unless cycled;
  RESETS to fw default on every board power-up)
- **Kind**: `cold` (power-on with partner attached) / `hot` (partner
  attached after boot) / `retrain` (in-session) / `soak`
- **Result**: `LIT` / `DARK` / partial; add golden-signature state for
  closed-loop builds (`K:03` etc.)

## Cable roster

| id | len | markings | Treedix CC | notes |
|----|-----|----------|-----------|-------|
| C1 | 6ft | USB3.2/USB4 Gen2 20G, 50V5A | CC1=1K, CC2=open | the "good" cable |
| C2 | 3ft | USB3.2/USB4 Gen2 20G, 20V5A | CC1=open, CC2=1K | low loss (short) |
| C3 | 6ft | USB4 Gen3 40G EPR, 20V5A | CC1=open, CC2=1K | premium/low loss |

Treedix = continuity/pin-map only; it CANNOT see channel loss or SI.
All three cables: all pins green incl. SBU; duplicate D+/D- red (normal).

## Build roster (flashed images referenced below)

| GW sha | built | config |
|--------|-------|--------|
| 5d8e15f0 | Fri 08-14 07:46 (a77e2422) | dp_test 2-lane HBR, CHAINED IP, pseudo-diff AUX, blind, 804mV. SecurityBit ON (pre-discovery) |
| d8c22f27 | Sat 08-16 | dp_test 2-lane HBR "plan C" un-chained, TLVDS AUX, closed-loop |
| 55571daf | Sun 08-16 | plan-C 2-lane + TLVDS + blind forced (lottery-check v2) |
| 74759364 | B2 | Sun 08-16 | CHAINED IP + pseudo-diff + blind (lottery-check v3 = streak replica) |

## Firmware roster

| FW | date | key properties |
|----|------|----------------|
| 98461af3 | Fri 08-14 17:15 | end-of-Friday. EQ default 1.0dB. Fri-PM source-role PD (VCONN/Rp/VBUS). Telemetry: short DP line (S/D/F/HLVC/P/E). NO: R/L/G/K/W/U fields, 'j','b' keys. In-slot hotplug: reported NOT working (user) |
| HEAD (10a4bf80+) | Sun 08-16 | EQ default 6.5dB. Full telemetry (R/L/G/K/W/U), VDM diag, jtag-toggle, muxbounce. Monitor-direct guilt UNPROVEN (confounded tests only) |

## Test table

Weekend backfill: only rows with certain provenance; `?` = not recorded
(the confounds that cost us two days). Rigor starts at #10.

| # | Board | date | GW | FW | Sink | Cable | Orient | Brk | Pwr | EQ | Kind | Result | Notes |
|---|------|-------|----|----|------|-------|--------|-----|-----|----|------|--------|-------|
| 1 | pre-switch | 08-14 AM | 5d8e15f0 | (Fri AM) | mon | ? | both | **Y** | mon? | 1.0 | cold+hot | LIT both orientations | "streak" era; 30-min soak passed; breakout inline ALL Friday |
| 2 | B1 | 08-15→16 | various | Sat/Sun | hubA | captive | n/a | N | bench (user 08-17: almost none in slot; 'slot' was mis-recorded) | 1.0→6.5 | many | mixed (2/5, 4/5, soaks clean) | closed-loop era; boot-draw model built here; conditions unlogged; 4/5 NOT reproducible 08-17 eve (row 18: 0/9 same bench) |
| 3 | B1 | 08-16 night | 55571daf | HEAD | mon | ? | ? | N | mon | 6.5 | cold ×2 | DARK 0/2 | TLVDS pad confound |
| 4 | B1 | 08-16 night | 74759364 | HEAD | ? | ? | ? | N | mon | 6.5→1.0 live | cold ×3 + EQ | DARK 0/3 | streak-replica GW but TODAY's fw |
| 5 | B1 | 08-16 night | 5d8e15f0 | HEAD | mon | ? | ? | N | mon | 6.5 | cold | DARK | "Friday binary" test — fw not reverted (invalid as Friday repro) |
| 6 | B1 | 08-16 night | 5d8e15f0 | 98461af3 | mon | C1? | one | N | mon | 1.0 | cold ×1 + replug ×5 | LIT 5/5 | BREAKTHROUGH row; one orientation only |
| 7 | B1 | 08-17 AM | 5d8e15f0 | 98461af3 | mon | C1 | ? | N | mon | 1.0 | cold ×5 | LIT 4/5 | cold morning |
| 8 | B1 | 08-17 AM | 5d8e15f0 | 98461af3 | mon | C2,C3 | both | N | mon | 1.0/6.5/9.5/12.3 | cold+EQ steps | DARK all | full EQ range dark on C3 both orientations |
| 9 | B1 | 08-17 AM | 5d8e15f0 | 98461af3 | mon | C3 | **both** | **Y** | mon | 1.0 | cold | **LIT both orientations** | ROOT CAUSE row: AUX overdrive needs attenuation |
| 10 | B1 | 08-17 | 5d8e15f0 (rewrite) | — | flash-tooling | — | — | — | mac | — | 3× flash populated | 2/3 first-attempt | loader a2mega2 SRAM-preload path; cycle-3 fail benign (no wedge) |
| 11 | B1 | 08-17 | 5d8e15f0 (rewrite) | — | flash-tooling | — | — | — | mac | — | 6× flash populated | 5/6 first-attempt | wait-only (OFL_GW5A_SETTLE_MS=3500); cycle-4 fail benign; ZERO replugs/wedges across all 9 cycles (#10+#11) |

| 12 | B2 | 08-17 | 74759364 | HEAD | flash-recovery | — | — | — | mac | — | SOM#1 revival | RECOVERED | fresh carrier (board B2) + SOM#1: ESP32 fw uploaded + WiFi provisioned (IP .116); preload-variant flash FAILED on corrupt flash (CRC err during JTAG load, ReadID fail — scanner interferes even with preload on pathological content); classic rescue path clean (JEDEC EF4017 = chip healthy, erase E:D, blank write 2:12 verify OK, auto-boot heartbeat). Saturday "SOM dead" verdict formally overturned |

| 13 | B2 | 08-17 | 74759364 | HEAD | mon | C3 | ? | **Y** | mon | 6.5 | cold | **LIT** | B2/SOM#1 first light. CLOSES: silicon-variation (dead — SOM#1 lights identical image), fresh-carrier bring-up (pass), Saturday-fw monitor guilt (exonerated — HEAD fw lights with AUX attenuation present) |

| 14 | B2 | 08-17 | c85d3afc | HEAD | mon | C3 | both | Y and N | mon | 6.5 | cold ×4 | DARK all 4 | TLVDS clean test NEGATIVE: spec ~350mV doesn't light monitor direct OR breakout (attenuating weak signal only hurts). Hub ACKed TLVDS all Saturday => sink-dependent AUX RX thresholds. Neither 1.0a3 drive is in the 0.4-1.38Vpp spec window at the connector. CLOSES 1.0a4 open item A: resistive level/bias network on the LVCMOS path (≈1V diff at connector + defined CM + RX bias), NOT the TLVDS pad |
| 15 | B2 | 08-17 PM | 74759364 | HEAD+net | hubA | ? | ? | N | hub | 6.5 | reload ×11 | K:00 all 11 | chained config on hub: AUX fine (D:2E) but ZERO symbol lock, deterministic — hub rejects chained outright (sink-dependence confirmed, high n). Via new telnet 'g' re-roll |
| 16 | B2 | 08-17 PM | d8c22f27 | HEAD+net | hubA | ? | ? | N | hub | 6.5 | reload ×10 + retrain | K:00 all | plan-C via fpgastream (exact Sat binary, .fs→.bin converted). 'g' reload alone leaves ladder idle (no HPD re-delivery — closed-loop builds need 'r' HPD pulse after reload); with 'r': training starts, collapses pre-CR |
| 17 | B2 | 08-17 PM | d8c22f27 | HEAD+net | hubA | ? | ? | N | hub | 1.0-12.3 | cold ×1 + EQ sweep | DARK, K:00 | fresh power-cycle of full chain: HPD present, training reaches channel-EQ (D:2A, L:8, G cycles) but NEVER completes, at ALL 4 EQ settings. NOTE: Sat 4/5 (row 2) was SLOT-powered — today's hub-powered bench persona was never in that dataset; persona or fw vintage = remaining deltas |
| 18 | B2 | 08-17 eve | d8c22f27 | HEAD+net | hubA | captive | n/a | N | bench 5V (JTAG conn) | 6.5 | power-cycle ×9 | BAD 0/9 (K:00, D:2A stall) | TRUE power-on draw series (quad common block re-rolls ONLY on power cycle — reload cycles in rows 15-16 re-rolled nothing). 6 cycles w/ capture-device HDMI sink + 3 w/ monitor HDMI sink: identical stall => HDMI sink exonerated. vs Sat 4/5: P(0/9|p=0.8) ~ 5e-7 => NOT the same process. Every sw variable eliminated (gw binary exact, fw both vintages, EQ all, attach flow). Remaining: slot power (supply ramp?) or Sat 4/5 miscounted. NOTE rows 15-17 'persona' framing WRONG: board was never hub-powered — external 5V via JTAG conn throughout; hub is bus-powered FROM board; hub cable is captive (C1-C3 are monitor-path only) |
| 19 | 08-18 AM | B1 | b9a908fb (39ada3ca first-success, streamed) | HEAD+net | hubA | captive | CC1 | N | hub-fed | 6.5 | power-cycle ×3 | **GOLDEN 3/3** (D:2E sustained, colorbars) | B1 cold overnight; SAME binary that went 0/12 on B2. Stall follows the ASSEMBLY (or warm/cold state — B2-cold control queued). Note B1 attaches CC1 vs B2 CC2 |
| 20 | 08-18 AM | B2 | b9a908fb (same, page-verified at write) | HEAD+net | hubA | captive | CC2 | N | hub-fed | 6.5 | power-cycle ×1 COLD | BAD (D:2A stall) | Cold control: B2 overnight-cold fails identically => thermal story DEAD. With row 19: **ASSEMBLY-DEPENDENCE CONFIRMED** (B1 3/3 golden, B2 0/13, same bits). Suspects: SOM TX margin / carrier channel+mux / BTB seating. Localizers: FFE-swing sweep on B2 (sw), cross-swap SOM1->carrier1 (hw), AD2 AUX amplitude compare |
| 21 | 08-18 AM | B2 | b9a908fb (FULL re-prep: fresh fw flash + fresh page-verified stream) | HEAD+net | hubA | captive | **CC1** | N | hub-fed | 6.5 | power-cycle ×1 | BAD (D:2A) | B2 fails on CC1 — the orientation B1 succeeds on. Re-prep rules out procedure |
| 22 | 08-18 AM | B2 | b9a908fb | HEAD+net | hubA | captive | CC2 | N | hub-fed | 6.5 | power-cycle ×1 | BAD | Orientation matrix complete: B1/CC1 3/3 golden; B2 dark on BOTH CC paths (0/16 cumulative). Crossbar hypothesis DEAD. Assembly hardware confirmed: SOM / carrier / seating. Next: FFE-swing sweep + SOM1->spare-carrier localizer |
| 23 | 08-18 AM | B1 | b9a908fb | HEAD+net | hubA | captive | CC1 | N | hub-fed | 6.5 | replug + 2 cycles | flap (9 trans/25s) -> golden-but-DARK -> hub power cycle -> **COLORBARS** | **HUB WEDGE FOUND**: hub externally powered since last night's oops = never reset; accumulates state that degrades DP->HDMI (flap, then golden-link-no-picture). B1 board side was golden throughout (38/38 D:2E stable while screen dark). Yesterday's bench series immune (bus-powered hub = auto-reset each cycle). NEW PROTOCOL: hub power cycle before every test block, or run hub bus-powered. Today's B2 rows 21-22 need fresh-hub retest |
| 24 | 08-18 AM | B2 | b9a908fb | HEAD+net | hubA | captive | ? | N | hub-fed | 6.5 | power-cycle ×1, FRESH hub | BAD (D:2A; led3/video_live dead, fabric alive+attached) | Fresh-hub control: B2 unchanged => assembly-dependence SURVIVES the hub-wedge confound. Final matrix: B1 golden+colorbars (fresh hub), B2 D:2A (fresh hub), same verified bits. Proceed to FFE/swing sweep |
| 25 | 08-18 | B2 | 04643444 (900mV) | HEAD+net | hubA | captive | ? | N | hub-fed | 6.5 | cycle ×1 | BAD D:2A | swing 804->900mV (txlev 15): NULL |
| 26 | 08-18 | B2 | 31b7ccfb (FFE 4.4dB + truthful decl + trunc fix) | HEAD+net | hubA | captive | ? | N | hub-fed | 6.5 | cycle ×1 | BAD, C:8011 | FFE pre-emphasis: NULL. But truthful TRAINING_LANE_SET declarations WOKE the hub's adjust dialogue (A:0000 -> A:0022 = swing2/preemp0 = exactly what we drive). preemp_level truncation bug found+fixed ([3:0]->[1:0] read swing bits; harmless once declarations truthful) |
| 27 | 08-18 | B2 | d377f594/e030f96a (per-lane C: + ppm Q:) | HEAD+net | hubA | captive | ? | N | hub-fed | 6.5 | cycles | BAD, C:8011 always | **VERDICT: BOTH lanes CR-only, symmetric = common-mode.** Refclk exonerated: Q = +7.5ppm (pristine). Every sw/config variable now nulled with data |
| 28 | 08-18 | B1 | e030f96a (same) | HEAD+net | hubA | captive | CC1 | N | hub-fed | 6.5 | replug attach | **GOLDEN: C:0177, G:F1, Y:11** | Reference trace: same hub request (A:0022), single-shot train, both lanes full lock, +5.2ppm crystal (2.3ppm from B2). FINAL: B2 hub stall = carrier-2 common-mode analog deficit FPGA->IT6563 (mux unit / passives / layout tolerance; refclk jitter unmeasured runner-up). NOT SOM, NOT software. Post-restart attach gap seen on B1 too (P:0 until hub replug) — task #1 |
| 29 | 08-18 | B1+B2 | e030f96a | HEAD+net | Ugreen hub, UtechSmart hub, USB->HDMI dongle, Anker | captive | - | N | JTAG 5V | 6.5 | multi-sink sweep | Anker: B1 **95%+ default-works** (rare dropouts) / B2 never; Ugreen: neither (LAYER UNKNOWN — HPD not checked); UtechSmart: neither + NO HPD (pin-assign-D suspect); dongle: neither (layer unknown) | **TWO findings, both real: (1) B1↔B2 assembly delta is LARGE (95% vs 0% on Anker — carrier-2 analog deficit stands); (2) other converters fail at a layer NOT YET CLASSIFIED — Ugreen/dongle could be PD/alt-mode (firmware) not training. NEXT: 2-min telemetry per sink on B1 classifies the layer (P:/HPD vs C:-stall) Mechanism (short reflective captive channels + bridge-grade RX vs long lossy cable + monitor RX) fits ALL data. UtechSmart no-HPD = likely the known pin-assignment-D firmware gap (find_dp_mode). WS4 (Gowin supported stack) = THE road for hub viability |
| 30 | 08-18 eve | B1 | a1a3a5a6+ | HEAD+net | Cable Matters USB->HDMI dongle | captive | CC1 | N | JTAG 5V | 6.5 | attach | CLASS 2: PD+HPD OK, ladder loops ERROR state (D:00) at DPCD presence read; AUX reply traffic present | Suspect: DEFER handling — ladder may treat legal DEFER replies as failure. Firmware/RTL fix candidate |
| 31 | 08-18 eve | B1 | a1a3a5a6 (check non-fatal) | HEAD+net | Ugreen | captive | - | N | hub-fed | 6.5 | attach | CLASS 4 verdict: GENUINE post-train loss, not phantom teardown (non-fatal held stream; C:8000 = sink reports ZERO locks; monitor 'no signal'). Locks TPS (C:8177) -> dies at scrambled normal mode | Old flap loop = correct ladder response |
| 32 | 08-18 eve | B1 | 36f44072 SCRAMBLER FIX | HEAD+net | Anker, then Ugreen | captive | - | N | hub-fed | 6.5 | regression + retest | Anker GOLDEN (C:0177 Y:11 — no regression); Ugreen STILL C:8000 | High-half SR fix real+sim-proven (NO K28.0 ever transmitted in 1080p builds before) but not Ugreen's killer. X:91 both hubs: ext-framing capable, downspread capable |
| 33 | 08-18 eve | B1 | 8166b65e downspread-truth | HEAD+net | Ugreen | captive | - | N | hub-fed | 6.5 | retest | STILL C:8000 | Truthful SSC declaration also null. Class-4 remaining: enhanced-framing-required (RTL project: 4-symbol sequences not implemented) / broadband margin (WS4) / deeper content — end-to-end post-scrambler verification harness is the systematic road |
| 34 | 08-18 night | B1 | 61ab95d6 (NEW toolchain 1.9.12 + SERDES 1.2 emission) | HEAD+net | Anker | captive | - | N | hub-fed | 6.5 | attach | **REGRESSION**: D3+D4 dark 20+s (symbol clock stalled at bring-up), then trains once, sink degrades to C:8001 (was C:0177) | Two-variable confound — split below |
| 35 | 08-18 night | B1 | 5b4c8e04 (NEW toolchain + 1.1 emission restored) | HEAD+net | Anker | captive | - | N | hub-fed | 6.5 | attach (hub repower needed) | **GOLDEN C:0177 ×37, G:F1, Y:11** | VERDICT: toolchain 1.9.12 CERTIFIED (clean timing, better Fmax margin, hw parity); **SERDES 1.2 emission = hardware regression** (20s bring-up stall + post-train degradation; likely its por_toggle_by_fabric flip + 0xc00008 3->1). Production stance: 1.1 emission + either toolchain. 1.2's fabric-POR lead needs surgical extraction, not wholesale adoption. Half-bond unfixed in 1.2 (Gowin ticket, 2 versions) |
| 36 | 08-18 late | B1 | 0d893690 prod-candidate, then ARCHIVED 5b4c8e04 (new TC) + 36f44072 (old TC) via git .fs->.bin | HEAD+net | Anker | captive | - | N | hub-fed | 6.5 | many replugs + power cycles | ALL THREE DEAD on late-night bench state (trains to C:8177 every round, sink drops ALL locks at video start; D3 blink = teardown echo). Same 5b4c8e04 bits were GOLDEN C:0177x37 ~1h earlier | **Software 100% exonerated by construction** (both toolchains, three binaries). Bench state drifted over hours of operation: prime suspect = un-heatsinked CARRIER MUX (TUSB1046A) thermal drift (SOM heatsink present on B1 all along); also hub warmth, seating wear. MORNING PROTOCOL: cold-boot the same 36f44072 bits untouched — colorbars=thermal (then localize w/ selective cooling), dead=seating. Retrain-restored build 0d893690 shows battle-loop on bad instances (correct behavior); non-fatal diagnostic reverted for production |
| 37 | 08-18 late | B1 | 36f44072 | HEAD+net | Anker | captive | - | N | hub-fed | 6.5 | **TUSB1046A cooled w/ compressed air** + replug | **GOLDEN C:0177 G:F1 Y:11 + COLORBARS** | **MUX THERMAL MARGIN CONFIRMED AT COMPONENT LEVEL**: one chip cooled = triply-dead bench -> golden. Reframes: (a) hub-path 'boot lottery' substantially = mux temperature at attach; (b) B1-vs-B2 delta plausibly per-unit mux thermal margin (B2 below line even cold, B1 above-cold/below-hot); (c) sink-strictness ladder rides on top (stricter sink = less thermal headroom). 1.0a4 CRITICAL: mux thermal design (thermal pad/via farm/airflow). Bench: heatsink both muxes NOW. QUEUED: Ugreen + cooled mux (class 4 may be thermal-conditional!), B2 + cooled mux (0/16 may flip!) |
| 38 | 08-18 late | B1 | 36f44072 | HEAD+net | **Ugreen** | captive | - | N | hub-fed | 6.5 | mux cooled + attach | **GOLDEN C:0177 G:F1 Y:11 + COLORBARS — CLASS 4 SOLVED** | The all-day 0-for-everything sink works on a cooled mux. Class 4 = class 1 with a stricter RX (less thermal headroom). Taxonomy collapses to: ONE analog axis (per-unit mux thermal margin × temperature) × per-sink RX strictness, PLUS two separate firmware gaps (Cable Matters AUX-DEFER, UtechSmart pin-assign-D). Scrambler/downspread fixes remain correct (spec compliance) but were not the blocker. NEXT: B2 + cooled mux |
| 39 | 08-18 late | B2 | e030f96a (in flash) | HEAD+net | Anker | captive | - | N | hub-fed | 6.5 | cold board, mux COOL to touch | BAD C:8011 (classic B2 stall) | **B2 deficit is temperature-INDEPENDENT** — fails with verifiably cool mux. Final model: B1 variance = mux-region thermal axis (cool=golden incl Ugreen; warm=video-start death); B2 = deeper per-unit deficit, same symptom family, bigger magnitude (suspects: voided thermal pad [die hot despite cool case — discriminate via rail current + warm-up rate vs B1], weak mux unit, caps, BTB seating). B1 mux was VERY HOT before air blast — abnormal for ~400mW part: voiding or electrical stress on B1 too. Morning: rail-current compare B1 vs B2, mux warm-up race, selective heating (mux vs caps) |
| 40 | 08-18 late | **B3 = carrier#3 + SOM#1** | 0d893690 prod-candidate | HEAD+net (crash-fix 1f56345e) | Anker | captive | - | N | hub-fed | 6.5 | first attach, factory-cold mux | BAD C:8011 (the SOM#1 signature, exactly) | **CROSS-SWAP VERDICT: DEFICIT FOLLOWS THE SOM.** Carrier 2 exonerated. SOM#1 = per-unit SERDES TX margin deficit (EQ-stall vs bridge sinks; monitors' tolerant RX unaffected) — silicon corner or wear from the flash-chaos/reseat saga. Final taxonomy: (1) per-SOM TX margin [SOM1 bad, SOM2 good]; (2) per-carrier mux-region thermal axis [carrier1 mux ran VERY HOT = voiding suspect] modulating good SOMs; (3) sink strictness ladder multiplies both; (4) fw gaps: DEFER (Cable Matters), pin-D (UtechSmart). SOM1 = quarantine from hub work (still fine for monitor-path/dev). New 60K SOMs ~2 weeks. 1.0a4/production: add per-unit hub-path screening test |
| 41 | 08-19 | B1 | 36f44072 | HEAD+net (1f56345e) | Anker | captive | - | N | hub-fed | 6.5 | SUPER-HOT mux: warm replug + 5x 'g' full-reconfig cycles (user-verified 5 blank->colorbars) | **6/6 WARM ACQUISITION GOLDEN** (C:0177 every cycle) | Same build/board/hub that failed repeatedly warm LAST NIGHT => **MARGIN-STACK MODEL**: acquisition margin consumed ADDITIVELY by mux temp (proven: cooling flip) + sink/hub fatigue (proven: row 23 wedge; last night = hot mux x cycled-to-death hub; today = hot mux x fresh hub) + per-SOM TX quality (row 40). Qualification = acquisition-rate MATRIX across {mux temp} x {hub freshness} x {SINK} — this row is the ANKER (most tolerant converter) cell only; Ugreen warm-acquisition UNTESTED (its only golden ever was cooled-mux). 'g'-harness VALIDATED as the instrument (acks + visible + hands-free). ALSO: 'r' HPD retrain proven inert (250ms pulse swallowed by fabric hotplug debounce — has never worked on any build; fw fix queued; prior 'r'-based conclusions re-read as ladder self-restarts) |
| 42 | 08-19 | B1 | 36f44072 | HEAD+net | Ugreen | captive | - | N | hub-fed | 6.5 | warm attach + 5 'g'; then COOLED attach + 5 'g' | warm: attach FAIL, g 0/5 (mix C:8011 EQ-stall / C:8000 — good SOM shows the SOM#1 signature when far below threshold: failure signature = depth gauge). **Cooled: physical attach = COLORBARS (2/2 lifetime)**, but g 0/5 even cold | **HARNESS SCOPING DISCOVERY**: 'g' reconfig = unannounced mid-stream link death; Anker re-acquires (6/6 warm), Ugreen NEVER re-engages without full renegotiation (PD/mux/HPD ceremony) even cold. g-cycles valid for Anker-class only; strict-sink attach statistics need renegotiation-inclusive events ⇒ FW FEATURE QUEUED: 'virtual replug' (PD alt-mode exit/re-enter). Matrix so far: Anker{warm:6/6}, Ugreen{warm-attach:0/1, cool-attach:2/2 lifetime, reconfig:0/5 any temp} |
| 43 | 08-19 | B1 | 36f44072 | HEAD+net | Anker | captive | - | N | hub-fed | 6.5->sweep | WARM EQ SWEEP: 5 g-acquisitions per setting | **6.5: 5/5, 9.5: 5/5, 12.3: 5/5, 1.0: 4/5** | Current 6.5 default VALIDATED (mid/high EQ all perfect at n=5; headroom exists). Low end cracks first — and 1.0dB-warm = 4/5 = EXACTLY the legendary Saturday figure (Friday-era fw defaulted EQ 1.0): the era's 'boot lottery' was plausibly EQ-setting x warm bench, now reproduced mechanistically. Deeper 6.5-vs-9.5-vs-12.3 differentiation needs n=20+ or enclosure-hot conditions |
| 44 | 08-19 | B1 | f70af283 HYBRID | HEAD+net | Ugreen | captive | - | N | hub-fed | 6.5 | WARM attach, NO cooling | **COLORBARS after visible 11-round battle (Y:BB), then stable C:0177 x30, mature** | First-ever warm-Ugreen success. Hybrid acquisition phase (retrain-until-caught) converged where the non-fatal build held dark after 1 attempt (row 42). Maturity ride-through now armed; soak running to observe advisory-mode behavior (G: low nibble counts wobbles without teardown) |
| 45 | 08-19 | B1 | f70af283 HYBRID | HEAD+net | Ugreen | captive | - | N | hub-fed | 6.5 | 90+ min WARM soak | **ZERO drops (Y:BB unchanged), C:0177 every sample, G:F5 static (no advisory events even), Q +9ppm fully heat-soaked** | Strongest qualification evidence in the log: strictest sink, warm, 1.5h perfect hold on the production-candidate policy. Tracking margin confirmed far above the stack; hybrid's acquisition win at attach was the entire battle |
| 46 | 08-19 | B1 | f70af283 HYBRID | HEAD+net | Ugreen | captive | - | N | hub-fed | 6.5 | POWER-CYCLE fresh boot, mux IR-measured 30C | **~20s battle (Y:FF, 15+ rounds) then stable C:0177** — user-observed 'came up after about 20 seconds' | Even cool, Ugreen acquisition margin is thin — but hybrid converts thin margin into a bounded delay instead of permanent dark. Product framing: worst case = picture in ~20s. NOTE: IR reading CORRECTED to ~46C (gun's laser misaligned with sense spot — aiming 'north' of the inverted chip's position reads the die area) — consistent with the 50-60C dissipation model and the very-hot-to-touch reports; thermal model stands. Measurement protocol note: calibrate the gun's laser offset before trusting spot readings on dense boards |
| 47 | 08-19 | B1 | f70af283 HYBRID | HEAD+net | Ugreen | captive | - | N | hub-fed | 6.5 | deep-warm bench (hours + battle-storm self-heat): power cycle -> 4+min no catch; g-rolls degrade 8177->8011; AIR: catches WHILE cooled, LOST on re-warm | **UGREEN OPERATING ENVELOPE MAPPED**: below acquisition line at deep-warm steady state (transient cooling = transient catch); catchable fresh/ambient (rows 44/46); holds forever once mature at any temp (row 45). Signature deepens with temp (8177-drop -> 8011-stall = the margin depth gauge live). NEXT: the 6x6 heatsink = sustained-cooling experiment; 1.0a4 margin items = durable fix |
| 48 | 08-19 eve | B1 | Gowin stack rounds 1-3 (b2d91ffb / c83456b8 / 0f416d7b / 75f0dabf) | HEAD+net | Anker | captive | - | N | hub-fed | 6.5 | 4 hardware cycles | Round 1: CR-only (C:8011). Rounds 2-3 fixes ALL silicon-verified (CR replay: 0x13110, 0x173, 0x20000 x2) but CR LOST (C:8000) and never returned | **WS4 HARDWARE CHAPTER CLOSED**: 3 generator-dropped hardened-mode fields found+fixed (mode enables 0xNN6C[13:12], analog 0xNNa0[9], PMA path 0xNNa6[17:16] — one coherent lanes-2/3 EDP-preset defect) + gear-1:1 hardened configs broken by construction + clock-meter duty artifact documented. Stack still dark post-fixes (NOT bench-confounded: hybrid achieved 8177 trains same bench/minutes). Residuals ranked in WS4 doc §11 (invert-in-hardened discriminator first). STRATEGIC VERDICT STANDS: our core = production; Gowin stack = ticket-enrichment only |
| 49 | 08-19 night | B1 | f70af283 HYBRID (restored) | HEAD+net | Anker -> HDMI: Sceptre swapped to **Fangor** | captive | - | N | hub-fed | 6.5 | deep-warm battle unresolved by air; HDMI-side hotplug swap -> CAME UP | **Two findings** (user-clarified: NO board/hub repower — HDMI cable swap ONLY; likely first pure HDMI-side hotplug in recent testing): (1) the SAME draw that battled unwinnably for 1h+ caught with zero source-side change ⇒ part of the failing state lives in the HUB'S DP-RX adaptation (goes stale/wedges during battles, source retrains can never clear it; downstream hotplug resets it WITHOUT power loss) — refines the deep-warm model (thermal + hub-RX-staleness are separate terms) and hardens the virtual-replug fw mandate (PD detach/re-attach = the source-side action that forces the hub's full reset); (2) **NEW content-layer bug**: Fangor shows colorbars shifted ~20px RIGHT with WRAPAROUND (rotation within the line = pixel stream ~20px out of phase vs blank-end framing, NOT monitor positioning — positioning clips, wrap = signal phase). Sceptre rendered same hub output correctly all week = Fangor takes timing literally where others re-sync. Suspect: latent off-by-N in our BE/H_START stream framing, absorbed by tolerant sinks. Tomorrow: (a) Fangor + known-good HDMI source control; (b) post-scrambler end-to-end sim harness (the scrambler-review recommendation) with exact border-position check |
| 50 | 08-19 ~2pm | B1 | f70af283 | HEAD+net | Anker -> Sceptre AND Fangor | captive | - | N | hub-fed | 6.5 | deep-warm: repower x2 | BAD on BOTH monitors (even Fangor which had just worked) | Full-system heat saturation: repower doesn't cool; fresh draws + both sinks all below line. USER PROTOCOL: everything unplugged for several hours = true full-ambient cold soak — the cleanest thermal-model test yet. Prediction on cold restart: catches (per morning-ambient behavior). Result -> next row |
| 51 | 08-19 eve | B1 | archived-binary bisect + v2/v3 | HEAD+net | Anker+Sceptre, FULL COLD | captive | - | N | hub-fed | 6.5 | cold restarts | **BISECT (all same cold bench): 36f44072 PASS, 8166b65e PASS (downspread-truth EXONERATED on Anker), 0d893690 PASS (new toolchain+retrain EXONERATED), f70af283 FAIL, hybrid-v2 (2ba67545, double-confirm) FAIL** -> hybrid ladder changes = the regression; both proposed mechanisms disproven (transient-double-confirm didn't fix; hotplug_decode present filter is spec-correct). **v3 (233972b3) = plain-retrain + all fixes: COLORBARS, resident production.** Maturity/ride-through feature demoted to SIM-FIRST dev (reproducer: IRQ-pulsing sink at video start, tb_channel_management). Cold-thermal model fully reinstated (the 'cold restart fails' scare was the hybrid regression, not thermal) |
| 52 | 08-19 late | B1 | 233972b3 v3 | HEAD+net | Anker -> **Fangor** hotplug | captive | - | N | hub-fed | 6.5 | HDMI hotplug | **COLORBARS, CORRECTLY POSITIONED — no offset** | Row-49 framing bug DOWNGRADED to watch item: the 20px shift+wrap was per-instance (hybrid build, stale hub, deep-warm — suspected inter-lane alignment slip that training tolerated), NOT a latent stream-engine off-by-N. Strictest monitor renders clean geometry on v3 under normal conditions. Watch for recurrence; no active investigation. Also: v3 + Fangor + Anker = another sink-combo pass for the production build |
| 53 | 08-19 late | B1 | 233972b3 v3 | HEAD+net | Ugreen+Fangor; Anker+both | captive | - | N | hub-fed | 6.5 | **mux HEATSINK fitted: 40C (was ~46C)** | Anker: fine both monitors. Ugreen: still no lock at 40C, locks after air — **AND THE 20px OFFSET+WRAP REAPPEARED on the cooled marginal catch** | Heatsink = -6C, clears nothing new (Anker bar >40C, Ugreen bar <40C). OFFSET UPGRADED from watch-item: correlates with MARGINAL acquisitions (2/2 sightings marginal, 1/1 clean-catch correct); both through converters+Fangor ⇒ leading theory = HUB-internal DP->HDMI raster-phase latch locks off-position under marginal DP lock (our stream innocent; Fangor renders honestly, Sceptre may re-center — check Sceptre geometry after a marginal catch). Product impact: marginal thermal state can yield shifted screen. Ugreen-class fix remains 1.0a4 margin work |

| 54 | 08-20 | **B3 (NEW roster: never-used carrier + GW5AST-LV138PG484AC1/I0 138K SOM; heatsinks on FPGA AND mux from first power-on)** | none (FPGA factory-blank) | HEAD (fresh ESP32 flash, this worktree) | none | - | - | N | JTAG 5V | - | first power-on; ESP32 flashed over USB, WiFi provisioned by user | **CARRIER-SIDE PASS with 138K seated**: PD stack up (SOURCE-WAIT-REQUEST SRC/DFP, CC1, HPD=0 MUX=0 EN=0 = correct idle), FUSB302 I2C alive, TUSB1046A I2C alive (08-0B: 05 00 09 00, 10-13: AA AA 00 00), WiFi/telnet/FTP up at 192.168.232.127, UARTDIAG EDGES=0 (FPGA silent = blank flash, expected) | ROSTER RENAME (user decision): "B3" now = this new 138K assembly; the row-40 "B3" (carrier#3+SOM#1) is henceforth the **"B2/SOM1" preserved assembly** — kept intact for later per-unit TX-deficit testing, SOM#1 still quarantined from hub work. Everything on the carrier that doesn't touch the fabric works identically with a 138K SOM; SERDES/refclk/AUX/fabric-pin verdicts await the 138B dp_test bitstream (subagent build in flight + SOM_138B_BALLMAP.md audit). Two other consoles on net (.62/.112) = previously flashed boards, identity TBC |

| 55 | 08-20 | B3 (138K, heatsinked FPGA+mux) | **a2mega_dp_test_138b 26f6c427, bin sha 6d3acc2f — FIRST-EVER 138K BITSTREAM** (GUI Serdes-1.2 emission Q0 ln1+ln2, 804mV/FFE-auto baseline, CSR byte-identical to vendor CLI verifier) | HEAD (IDCODE-aware fw 85ffd798) | Anker+monitor | captive | - | N | JTAG 5V | 6.5 default | first flash + hub replug | **GOLDEN C:0177 HLVC:1111 G:F0 + COLORBARS on first replug, no battle** | **138K CARRIER COMPATIBILITY SILICON-VERIFIED**: ball-map audit (SOM_138B_BALLMAP.md, "compatible as-routed") confirmed end-to-end — PD/mux/JTAG-flash/fabric/rebuilt-PLLs/AUX-closed-loop/SERDES on die lanes 1+2 all work; 1.0a4 freeze item CLOSED, no copper change. Serdes 1.2 emission fine here (no 60K-style 1.2 regression); 804mV/FFE-auto sufficient for Anker path on this SOM. TWO FLASH LESSONS: (1) fw IDCODE gate refused the 138K — fixed by image-derived IDCODE match (85ffd798, also blocks wrong-die flashing both directions); (2) **NEW BENCH LAW: WiFi streams REQUIRE Mac USB detached** — with a USB host enumerated, the ESP32 USB-Serial-JTAG peripheral defeats the bit-bang handoff (IDCODE reads ffffffff, all retries; detach → first-try entry). WATCH: Q: field reads 80BF68x (not 60K-style small ppm) — likely scaling from the rebuilt 148.5 pixel PLL, video unaffected. Ugreen/Fangor matrix on 138K pending |

| 56 | 08-20 | B3 (138K) | 26f6c427 (804mV/FFE-auto) | HEAD | Anker + Fangor↔Sceptre HDMI hotplugs | captive | - | N | **hub-backfed VBUS (JTAG power did not work on this board — all B3 data this session is hub-powered)** | 6.5 | HDMI hotplug to Fangor after ~25min runtime | NO VIDEO — link died and stuck C:8010 (EQ-stall); back-swap to Sceptre also C:8010; **'g' full reconfig did NOT recover; quick power cycle DID** → colorbars **BUT VERTICALLY OFFSET WITH WRAPAROUND — ON THE SCEPTRE** | **OFFSET BUG RE-CHARACTERIZED, SINK-INDEPENDENT**: row-53 discriminator answered — Sceptre does NOT re-center; the marginal-catch offset renders on any monitor, now seen on BOTH axes (horizontal 60K/Fangor, vertical 138K/Sceptre), full-frame rotation (wrap, content perfect) = frame-start phase latched wrong at marginal acquisition. Product exposure broader than filed: any converter-path marginal catch can ship a shifted screen. "Our stream innocent" claim must re-earn: post-scrambler end-to-end sim w/ border-position check (row-49 to-do) PROMOTED back to active. 138K-specific alt suspect if clean catches ever shift: rebuilt pixel PLL init-off-gated-word-clock caveat. **UPDATE (post-row-57 clean catch): 30s-drain catch renders PERFECT geometry on Sceptre — correlation sealed: marginal catches rotate (3/3), clean catches true (all checked), across 2 dies/2 axes/2 monitors; pixel-PLL suspect stands down unless a clean catch ever shifts** |
| 57 | 08-20 ~10:30am | B3 (138K) | 26f6c427 (804mV) | HEAD | Anker + Sceptre | captive | - | N | hub-backfed VBUS | 6.5 | session warm-up arc, then air + recovery ladder | Depth-gauge arc over only ~90 MIN from morning-ambient start on a HEATSINKED, relatively COOL board: first-attach golden → post-battle C:8010 → C:0000/A:0000 (deepest); **air on mux moved gauge 0000→8010 (thermal term REAL) but did NOT close link**; quick hub power cycles (= full PD renegotiation) did NOT recover; **30-SECOND hub power-off DID → colorbars** | **TOLERANCE VERDICT (user framing, adopted): small thermal deltas flipping outcomes on a cool dual-heatsinked board = the operating point sits ON the margin line at 804mV — a tolerance/margin-budget problem, not a cooling problem.** Fix = move the operating point: (a) 900mV/FFE-manual (60K production drive) A/B queued — 5-min regen + rebuild; (b) cold-morning baseline on 804mV first (one variable). ⚠ RECOVERY FINDING: the wedge survives QUICK power cycles (PD renegotiation insufficient!) but clears on 30s drain ⇒ **virtual-replug fw feature may NOT clear this class** — hub state deeper than PD; product recovery ladder needs the 30s-unplug rung until margin work makes it rare. NOTE (CORRECTED 08-20): earlier wording here claimed a B3 JTAG-power failure — wrong; JTAG power was never used on B3 and not lately on any board. The real open issue is the a2mega power topology one (see requirements item 4b): non-VBUS 5V ingress (edge+LM74700 or SOM-JTAG-5V-via-BTB) correlates with an 'unhappy' USB-C connection, failure mode and cause both uncharacterized |

| 58 | 08-20 | B3 (138K) | flashing saga (900mV bin efc16352) | 364c4ccf→185d8844 evolution | - | - | - | - | hub-backfed VBUS | - | 4 WiFi stream attempts + diagnostics + PC path | **WiFi STREAMING TO A CONFIGURED 138K = BROKEN AT THE DIE LEVEL; PC/USB PATH WORKS** | Root-caused via staged fw diagnostics (IDCODE-match fix → JEDEC passthrough verify → keepsram trial → per-try WiFi telemetry): with a VALID image in flash, the 60K-derived GW5A SPI-enable never yields a live passthrough on GW5AST-138 (JEDEC reads FFFFFF with JTAG alive), and the KEEPSRAM flavor WEDGES THE TAP outright (IDCODE ffffffff until power cycle). Blank-flash streams work (morning 804 flash = the one lucky state). Erases never executed (heartbeat survived power cycles = flash intact — the diagnostic key, user-spotted). openFPGALoader via ESP32 USB-JTAG bridge (tools/flash.sh, GPRJ=a2mega_dp_test_138b.gprj) flashed+verified 100% clean ⇒ its GW5A flash-access sequence does what ours doesn't on the 138 die — reference for the eventual fw fix. UNTIL THEN: 138K build swaps = Mac-attached USB flash; WiFi 'g' reload unaffected |
| 59 | 08-20 | B3 (138K) | **900mV/FFE-manual efc16352 FIRST HW RESULT** | 185d8844 | Anker+monitor | captive | - | N | hub-backfed VBUS | 6.5 | deep-warm bench (hours, dozens of cycles): cold-boot attach, then sustained air on mux | C:8010 battling (Y:AA, A:0022 healthy) — same as 804 warm; **air: gauge did NOT move (vs row 57's 0000→8010)** | Bench is in the row-50 FULL-SYSTEM SATURATION regime — everything fails regardless of build/cooling; A/B UNINFORMATIVE in this state, halted deliberately (no burning draws on a saturated bench). UPDATE: sustained air brought mux to MEASURED 35C (below every B1 acquisition bar) — gauge still unmoved ⇒ MUX TEMPERATURE EXONERATED for this state; prime suspects now hub-wedge (30s-drain test next) and the untested-on-138K 900mV/FFE-manual combo itself (FFE coefficients validated on 60K silicon only; 804 flashback = discriminator via USB path). Drive A/B DEFERRED to cold protocol: multi-hour unplugged soak, then cold-start 900mV, compare vs 804's cold-golden (row 55); build swaps need USB flash (row 58). NOTE: 900mV build boots/trains/AUX-exchanges normally — no regression from the drive change itself |

| 60 | 08-20 | B3 (138K) | 900mV efc16352 | 185d8844 | Anker+Sceptre | captive | - | N | hub-backfed VBUS | 6.5 | 30s HUB DRAIN + power-on (mux ~35C from earlier air) | **COLORBARS, golden C:0177 HLVC:1111 — 900mV build's FIRST catch — but OFFSET IN BOTH AXES, WRAPPED (Y:22 = marginal, post-battle catch)** | 30s-drain recovery reproduced (row 57's recipe, now on 900mV; hub-wedge was the controlling term for the late-session failures — mux temp already exonerated at 35C). OFFSET BUG n=4: h-only(60K/Fangor), v-only(138K/Sceptre), BOTH-axes(this row); ALWAYS clean wraparound; ALWAYS marginal catches; clean catches always true. **THEORY UPGRADED (user: "internal counters problem — wrapping too clean"), by-construction analysis concurs: top-level pattern is a pure function of the transmitter-supplied (cx,cy), so rotation must arise INSIDE dp_transmitter — pixel-request counters (clk_pixel) vs framing counters (symbol domain) phase-latched at a mid-battle video start (CDC/FIFO priming). Hub demoted to innocent renderer.** Sim investigation spawned (tb_video_restart base, border-position reproducer) |

| 61 | 08-20 | B3 (138K) | 900mV efc16352 | 185d8844 | Anker+Sceptre | captive | - | N | hub-backfed VBUS | 6.5 | g re-roll (failed → battle, D4 3-pulse, D3 teardown-echo blink) then 30s FULL drain (hub+hdmi+power) + replug | **PAIRED SAMPLE COMPLETE: clean catch Y:11 → CORRECT CENTERED GEOMETRY** (vs row 60's marginal Y:22 → both-axes rotation) — same session/build/hub/monitor, predicted before user looked | Offset correlation now n=5, perfect, with a CONTROLLED PAIR: marginal catches rotate (4/4), clean catches center (every one, incl. this predicted-in-advance sample). Also: 'g' re-acquisition FAILED on B3+Anker (contrast B1 warm 6/6) — the unannounced link death wedged the hub into the drain-only state again; 30s-drain recipe now 3/3 as the sole reliable recovery. Y: field = marginality gauge validated (11=clean, 22+=battled). Sim agent investigating the video-start counter-phase mechanism (row 60) |

| 62 | 08-20 | B3 (138K) | **6aa2660b = 900mV + OFFSET FIX (video_stream_packer SOF-invariant, 6a0eaa3f)** | 185d8844 | Anker+Sceptre | captive | - | N | hub-backfed VBUS | 6.5 | USB-path flash (first attempt failed: ESP32 not enumerated — cable reseat fixed); power cycle → battle (C:8010 Y:66, quick cycle didn't clear hub) → 30s full drain → catch | **GOLDEN C:0177 with Y:44 — a BATTLED/MARGINAL catch — GEOMETRY CORRECT/CENTERED** | **FIRST HARDWARE CONFIRMATION OF THE OFFSET FIX**: pre-fix, marginal catches rotated 4/4; this Y:44 catch is exactly the class that rotated, now centered. Fix chain: root cause (packer one-shot SOF alignment + silent FIFO word slip) → sim reproducer (7/10 scenarios rotated on old RTL) → v3 fix (continuous SOF invariant + starved-fetch resync) → patched sims 10/10 (0,0) + vr2 regression clean → hardware. Confidence grows with every future marginal catch that centers; falsifier = any rotated session on a fixed build. Phase V1 of PRE_REV_EXERCISE_PLAN effectively COMPLETE (accumulating n). Also: 30s-drain recipe now 4/4 |

| 63 | 08-20 | B3 (138K) | 6aa2660b (900mV+offset fix) | 9db6fbad (16-step EQ '+'/'-') | Anker+Sceptre | captive | - | N | hub-backfed VBUS | **full ladder sweep** | post-30s-drain catch, then live hold-sweep: 0→15→0, ~3s dwell/step | **GOLDEN THROUGH ALL 16 SETTINGS BOTH DIRECTIONS — C:0177 all 150 samples, Y:11 frozen, colorbars rock-solid, zero flicker; clamps verified both ends** | **TRACKING MARGIN SPANS THE ENTIRE EQ LADDER (1.0→14.4dB)** at bench temp — once locked, NO setting breaks the link; strongest confirmation of the tracking-margin-above-stack model. ⚠ BOOKKEEPING CORRECTION: fw boot default = SETTING 0 (1.0dB), NOT 6.5 — the EQ column in rows 55-62 is wrong; ALL B3 catches/battles to date ran at 1.0dB default (incl. post-drain catches at the bar B1-warm found marginal). Remaining EQ science = ACQUISITION-per-setting scan (needs fresh attach per setting ⇒ wants virtual-replug fw or drain patience). Also demonstrated live mid-battle EQ stepping shifts the depth gauge (8010→8071 pre-drain) |

| 64 | 08-20 | B3 (138K) | 6aa2660b | 75362b84 (M1 'l' toggle) | Anker+Sceptre | captive | - | N | hub-backfed VBUS | 0 (1.0dB default) | M1 first A/B: colorbars up, mux 35C (board still warming), 'l' toggle → DPCTL3=0x9C read back (snoop-dis + SBU-ovr + DP2/3 DISABLED) | **VIDEO SURVIVED THE TOGGLE (driven-input assumption VALIDATED: ours = mux DP0/DP1); mux 35C → 33-34C** | M1 DIRECTIONALLY CONFIRMED: temp fell 1-2C WHILE the board was still warming toward its usual 40C steady state ⇒ true steady-state delta likely larger, magnitude TBD (clean protocol: toggle at full equilibrium ~40C and watch descent). Idle-pair bias was real load. Candidate for default-on fw policy after the equilibrium measurement + a warm-attach count. UPDATE (session end ~9:30pm): mux STILL HOLDING 33-34C well past normal settle time — the toggle appears to have ARRESTED the climb toward the usual ~40C operating point (~6C class delta, heatsink-sized). Cross-session-baseline caveat stands; definitive same-session A/B = the reverse toggle ('l' back on, watch climb) — QUEUED for next session, one keystroke. If ~6C holds, steady-state mux drops BELOW the Ugreen acquisition bar (<40C) ⇒ M1 alone may flip the strict-converter class; Ugreen-with-lanes-off retest promoted in V2. Also noted: pre-config mux EQ regs read AA/AA (floating-strap setting 10 = 12.3dB) until fw writes land — consistent with the strap-latch comment in usbc_glue |

| 65 | 08-21 AM | B3 (138K) | 6aa2660b (900mV+fix) | 75362b84 | Anker+Sceptre | captive | - | N | hub-backfed VBUS | 0 (1.0dB default) | COLD PROTOCOL (21C ambient): 3 rapid power-on draws + 1 post-30s-drain draw | **cold catches 3/4: cycles 1,2 golden Y:33 centered; cycle 3 STALLED C:8010 Y:88+ at mux 33C (>2min); post-drain cycle 4 golden Y:33 centered** | 900mV COLD BASELINE: catches reliably off fresh hub state; the one stall = hub fatigue from rapid cycling (drain cleared it — recipe now 5/5 lifetime). SIGNATURE NOTE: all three 900mV cold catches ran Y:33 (short battle then catch) vs 804's single cold sample Y:11 (clean, row 55) — small n, but consistent; hypothesis: 900mV slightly OVERDRIVES the short channel cold (more swing = bigger launch reflections, coherent with the 5a reflection model). 804 flashback comparison remains the discriminator. Offset fix: 3 more battled-catch centered renders (fix n grows). Next: warm to equilibrium (lanes ON stock) for the M1 delta measurement |

| 66 | 08-21 12:30p | B3 (138K) | 6aa2660b | 75362b84 | Anker+Sceptre | captive | - | N | hub-backfed VBUS | 0 | **M1 EQUILIBRIUM MEASUREMENT (clean protocol)**: 3h natural warm-up at STOCK all-four-lanes (cold→34→38C), then single 'l' toggle at 38C, ~15min descent | **38C → 33C = 5C DELTA, same session, one variable; link golden throughout, video untouched** | **M1 DELTA CONFIRMED ~5C** (heatsink-magnitude, from one register write). Lanes-off steady state = ~33C — BELOW the B1 Ugreen acquisition bar (<40C, air-only territory). DEFAULT-ON candidacy now rests on the warm-attach counts + Ugreen class-flip test (queued next, after the V2.1 flash window: attach → 'l' → 'v' gives a lanes-off ceremony without cable dances). Note for protocol design: board is hub-backfed, so every physical hub/cable swap = board power cycle = lanes reset to stock — 'v' (virtual replug) is the instrument that makes lanes-off attach experiments possible |

| 67 | 08-21 ~1pm | B3 (138K) | bcda0527 (900mV+fix+hpd-reset) | V2.1 fw (v replug) | Anker then **UGREEN (B3's first)** + monitors | captive | - | N | hub-backfed VBUS | 0 | V2.1 verify + warm draws + Ugreen campaign: 'r' test, 'v' x5 draws (2 Anker lanes-on, 1 Anker lanes-off, 2 Ugreen incl lanes-off), 3x 30s drains, Ugreen untouched-baseline attach | **'r' WORKS (first time ever — visible blank+retrain, HPD-reset hw-verified); 'v' ceremony works (detach+renegotiation, board survives CC-open on backfeed); post-flash Anker catches golden (Y:22, Y:11 — HPD-reset regression gate PASSING). BUT: v-draws 0/5 warm; UGREEN 0/2 attaches + 0/2 draws in EVERY config (lanes on/off, cool/warm, fresh drains) — stuck C:8071 (deeper than Anker's 8010; A:0026 adjust pattern)** | V2.1 SHIPPED AND VERIFIED. Ugreen verdict: **B3/138K@900mV cannot catch the Ugreen AT ALL — the sink B1/60K caught when cool.** Temperature exonerated (33C lanes-off ≠ catch), lanes exonerated ⇒ prime suspect = 900mV DRIVE (cold Y:33 overdrive hint aligns; strictest RX = most overdrive-sensitive). M1 class-flip hypothesis NOT confirmed (thermal value stands, acquisition value unproven). DISCRIMINATOR QUEUED: 804 flashback (archived 6d3acc2f) via FS= USB flash → drain → Ugreen attach. Also: 'v' is the renegotiation instrument but wedge-class still needs drains (30s recipe now 8/8) |

| 68 | 08-21 | B3 (138K) | **804mV archived 6d3acc2f (PRE-fix, PRE-hpd-reset)** | V2.1 fw | **UGREEN** + monitor | captive | - | N | hub-backfed VBUS | 0 | 804 flashback via FS= USB flash → 30s drain → untouched first attach | **COLORBARS — B3's FIRST-EVER UGREEN CATCH — golden C:0177 Y:22 on first attach. Frame displaced both axes, wrapped (build predates the offset fix)** | 🏆 **DRIVE A/B DECISIVE: same board/day/sink/ceremony — 900mV = 0-for-all (row 67), 804mV = first-attach catch. 900mV OVERDRIVE is the B3 Ugreen killer** (cold Y:33 hint vindicated; strictest RX = most overdrive-sensitive; coherent with 5a launch-reflection model — more swing = bigger reflections on the short channel). ACCIDENTAL CONTROL: offset reproduces on the pre-fix build at a marginal catch (correlation n=6; the fixed build's rows 62/65/66 never rotated — counterfactual demonstrated). CONSEQUENCES: (1) 138B production candidate = 804mV + offset fix + HPD reset — needs IP regen back to 804/FFE-auto + rebuild; (2) OPEN LEAD: check whether B1/60K production also carries the 900mV drive experiment — if so its thermally-conditional Ugreen behavior may be the SAME overdrive, and 804 might catch warm there too; (3) M5 runtime AFE (per-sink drive) upgraded from margin lever to likely necessity |

| 69 | 08-21 | B3 (138K) | **3c8ac385 PRODUCTION CANDIDATE (804+fix+hpd-reset+coherent ROM)** | V2.1 fw | UGREEN + monitor | captive | - | N | hub-backfed VBUS | 0 | flash → drain → attach (draw1) → power cycle (draw2) | **draw1 = RESET STORM (Y:FF saturated, C:0000/A:0000 — restarts outpace training; qualitatively unlike real battles); draw2 = GOLDEN Y:33 CENTERED** | Candidate CAN catch the Ugreen (804 drive confirmed on the new build) and the fix renders TRUE on a battled strict-sink catch (offset correlation n=7, zero exceptions — validation complete across the roster). NEW FAILURE MODE CHARACTERIZED: the HPD-disconnect reset can enter a RESET STORM on attaches where the sink flaps HPD/status (feedback: flap→reset→retrain→flap) — plausibly THE hybrid-regression mechanism (row 51) finally seen live with a telemetry signature. Storm-vs-catch is per-draw; isolation build 8db62fa3 (reset OFF) built+ready as comparison arm. NEXT: classify v-draws per build (storm rate A/B), then redesign the reset (train-phase masked / rate-limited) |

| 70 | 08-21 | B3 (138K) | 3c8ac385 candidate | V2.1 fw | **UGREEN** + monitor | captive | - | N | hub-backfed VBUS | 0 | **6x automated 'v' draws (full detach/renegotiation ceremonies), telemetry-classified** | **6/6 CATCHES — Y:11 clean x5, Y:33 x1 — colorbars up and CENTERED on every draw (user-verified)** | 🏆 **HEADLINE OF THE MARGIN CAMPAIGN: the Ugreen — 0-for-lifetime on warm draws across every board — is RELIABLE at 804mV.** The strict-converter class was OVERDRIVE, not thermal margin (rows 37-53's temperature sensitivity = how much overdrive the RX could stomach at a given temp). With correct drive: dependable renegotiation re-acquisition on the strictest sink, warm bench, no drains needed. Candidate running total: 7/8 draws (1 reset-storm on the cold power-on attach — storm not yet seen in any v-ceremony draw; watch whether it is attach-path-specific). 'v' draw automation + telemetry classification = the acquisition-statistics instrument working exactly as designed. Offset fix: 6 more centered catches (n=13, zero exceptions) |

| 71 | 08-21 | B3 (138K) | 3c8ac385 candidate | V2.1 fw | UGREEN + monitor | captive | - | N | hub-backfed VBUS | 0 | 5x manual power-cycle draws (user screen-truth + telemetry quality) | **5/5 CATCHES, ALL CENTERED — Y:33 x4, Y:11 x1; zero storms** | **PRODUCTION CANDIDATE CONFIRMED: 12/13 total draws on the Ugreen** (6/6 v-ceremonies + 5/5 power-cycles + 1/2 first-attach where the sole reset-storm lives — first-post-flash-attach only, rate too low to dethrone the build). 3c8ac385 = the 138B production build: 804mV/FFE-auto, offset fix, HPD-disconnect reset (functional 'r'), coherent replay ROM. Reset-storm redesign (train-phase masked / rate-limited) stays queued as hardening, not a blocker. Ugreen reclassified: RELIABLE sink at correct drive. Offset fix final tally this arc: n=18 catches, zero rotations |

| 72 | 08-21 6pm | B3 (138K) | 3c8ac385 | V2.1 fw | Anker+Sceptre | captive | - | N | hub-backfed VBUS | 0 | cold power-on (2-3h cooldown) — the cold-Anker regression gate | **RESET STORM (Y:FF/C:0000/A:0000), 'v' did NOT clear it, then SELF-RESOLVED to golden (C:0177, Y:FF tombstone) with no intervention** | **STORM MECHANISM REFINED: downstream-monitor-asleep HPD flapping.** All storms (n=2) = attaches with a sleeping monitor (cold boot; post-flash); all storm-free draws = awake monitor. Hub flaps HPD probing its sleeping sink → each flap resets our ladder → dark until the monitor stabilizes, then next attempt trains. Old ladder trained THROUGH the flaps (video ready at wake). 'v' cannot clear an active storm (the flap source persists). **COLD-ANKER REGRESSION GATE: FAILED → HPD_DISCONNECT_RESETS OFF in production. 8db62fa3 (804+fix, reset off — built, unflashed) = new production candidate**; flash next USB window + quick revalidation (Ugreen v-draws + cold Anker). 'r' dormant again; redesigned reset requirement now precise: ignore flap-rate HPD drops, reset only on sustained disconnect (also explains the hybrid: same storm at video start on IRQ-raising sinks) |

| 73 | 08-21 eve | B3 (138K) | **8db62fa3 = 804mV + offset fix, HPD-reset OFF** | V2.1 fw | Anker+Sceptre | captive | - | N | hub-backfed VBUS | 0 | flash + cold-ish power-on (the storm build's failing scenario) | **COLORBARS in seconds — two D4 blinks (plow-through attempts during the flap window) then golden Y:33 C:0177** | Direct contrast with row 72: same scenario that stormed 3c8ac385 for minutes trains in seconds on the reset-off build. **8db62fa3 = 138B PRODUCTION** (Ugreen v-draw revalidation to complete the stamp next session — expected clean: the reset was the only delta from the 12/13 config's behavior-relevant path, minus 'r'). Day closes: overdrive verdict, production build, V2.1 arc complete (v stays, r dormant pending flap-tolerant redesign), M5 designed, M1 measured |

| 74 | 08-21 eve | B3 (138K) | 8db62fa3 PRODUCTION | V2.1 fw | **UGREEN** swap-in | captive | - | N | hub-backfed VBUS | 0 | physical hub swap (= power cycle) + first attach | **COLORBARS FIRST TRY** | **PRODUCTION STAMP COMPLETE: 8db62fa3 catches BOTH hubs on first attach, same evening.** A-field decode recorded for M5: Anker requests A:0022 (VS2/PE0 both lanes), Ugreen A:0026 (VS2 + PE1 on lane 0) — both sinks have been requesting ~800mV all along; the Ugreen additionally wants pre-emphasis we've never granted. USER DECISION: proceed with M5 (honor ADJUST_REQUEST) rather than chase a fixed goldilocks point that breaks on the next sink/cable — M5 implementation begins |

| 75 | 08-21 eve | B3 (138K) | **972110be = M5 FIRST HARDWARE (804 base + fix + runtime AFE adjust, M: telemetry)** | V2.1 fw | UGREEN | captive | - | N | hub-backfed VBUS | 0 | USB flash → power-on attach + 4 classified v-draws | **5/5 CATCHES, all battled (Y:77/88/88/AA); A:0033 every draw (Ugreen now requests VS3/PE0 both lanes — was 0026 = VS2+PE1); M:03 = VS3/PE0 applied, seq_err clear, known=0 (clears by design when training ends)** | **M5 WORKS MECHANICALLY** (request parsed → DRP applied → truthful declaration → sink responds). BEHAVIORAL FINDING: truthful "VS2, more available" invites the sink to ESCALATE to max swing; we apply VS3 = 900mV/C1=0 and the link trains WORSE (every catch battled vs production 8db62fa3's clean Y:11s). Also: 900mV+C1=0 DOES catch ⇒ the old 900 build's 0-for-all was the 900 + C1=8 COMBINATION (both hurt; which more = open). DESIGN DECISION: our 900mV max sits nearer DP nominal VS2 (800) than VS3 (1200) ⇒ CLAMP requests to VS2 + flag MAX_SWING at VS2 (the design's §4 reporting flip) — expected: sink settles at VS2, asks PE1 as before, and this time GETS it (M:16 on 804 + 3.7dB) = the untested 804+PE1 config. Rebuild queued |

| 76 | 08-21 night | B3 (138K) | **84be7590 = M5 + declared-ceiling clamp (MAX_VS=2)** | V2.1 fw | UGREEN | captive | - | N | hub-backfed VBUS | 0 | USB flash → power-on + 4 classified v-draws | **5/5 catches; A:0022 every draw (VS2/PE0 — NO PE1 request this time), M:02 applied; but Y:77/99/88/99/99 — every catch battled** | CLAMP WORKS (no escalation, sink settles at VS2 — the row-75 0026/PE1 was likely a mid-training snapshot, not a settled preference). BUT the catches battle ~8 rounds where production 8db62fa3 catches Y:11-33 AT THE SAME LEVELS ⇒ the perturbation is the ACT of applying: INIT fires an 8-write DRP sequence + APPLY strobe at EVERY training start, and it converts the boot csr's FFE AUTO into manual/C1=0 — an analog change vs production (auto-FFE may contribute useful EQ). ISOLATION A/B queued: AFE_APPLY_ON_START=0 (trust the boot csr, DRP only on a real sink change) ⇒ for the Ugreen = zero DRP activity ⇒ should equal production's clean catches exactly |

| 77 | 08-21 night | B3 (138K) | **48576c12 = M5 + clamp + AFE_APPLY_ON_START=0 (trust boot csr; DRP only on a real sink change)** | V2.1 fw | UGREEN | captive | - | N | hub-backfed VBUS | 0 | USB flash → power-on + 4 blind v-draws (screen-truth) + 3 instrumented v-draws | **8/8 catches; power-on Y:22, then Y:33 settled, every instrumented v-draw Y:11 FLAT across all samples; A:0022 / M:02 / C:0177 throughout; zero DRP activity (sink request == INIT)** | **INIT-PERTURBATION HYPOTHESIS CONFIRMED** (row 76): same RTL, same levels, only difference = no INIT DRP strobe/FFE auto→manual flip at training start ⇒ battles (Y:77-99) collapse to the cleanest readings this board has produced (Y:11 flat beats production 8db62fa3's Y:11-33). M5 PRODUCTION POLICY SETTLED: the boot csr (804 mV / FFE AUTO) is the baseline; the AFE sequencer only writes when a sink asks for something different (tb_afe_noinit proves zero writes at INIT, apply-on-change, re-apply on return). Ugreen exercises only the no-write path; the write path remains sim-proven + row-75/76 hardware-proven mechanically. NEXT: Anker cross-check on this build (expected: same A:0022 no-write path), then Q: does the sequencer's apply path itself (8 writes + strobe mid-training) cost margin when it IS exercised — answer needs a sink that asks for ≠INIT (PE1/VS1) |

| 78 | 08-21 night | B3 (138K) | 48576c12 (M5, trust-boot-csr) | V2.1 fw | **ANKER** swap-in | captive | - | N | hub-backfed VBUS | 0 | physical hub swap (= power cycle) + 3 instrumented v-draws | **3/4 by SCREEN-TRUTH: power-on after 6 D4 blinks (Y:88); v-draw 1 latched (Y:11), v-draw 2 did NOT latch (telemetry went C:0177 golden but Y climbed 55→88 afterwards and no picture), v-draw 3 latched (Y:22); A:0022 / M:02 throughout** | ⚠ TELEMETRY BLIND SPOT (user caught it): C:0177 = symbol-level training success, NOT picture-on-screen; a post-golden rising Y is the tell for a phantom catch — classify draws by screen-truth, never by C: alone. M5 cross-check PASSES: Anker also requests exactly INIT ⇒ no-write path, M5 adds nothing and costs nothing. Marginality band matches the Anker's whole 804 history (row 73 production: 2 blinks, Y:33, n=1 — not distinguishable from tonight's n=4). Anker remains THE marginal sink at 804: it asks for VS2/PE0 yet battles — the lever it isn't pulling is PE, and we can't offer it without a sink request. Note: draw 2's Y climbed 55→88 AFTER the catch (post-catch marginality counting), so Y-at-catch ≠ final Y on this sink. HUB RANKING FLIPPED at 804: the Ugreen (historically the strict converter) now outperforms the Anker on blinks-to-colorbars — consistent with the overdrive verdict (Ugreen's problem was 900 overdrive; the Anker tolerated 900 and is marginal at 804). This IS the goldilocks problem M5 was meant to solve, and M5 cannot solve it while both sinks request the same VS2/PE0. NEXT: hub-neutral margin levers are now M2 (EQ acquisition scan per setting, cheap with 'v') and M7 (bonding/skew) — M5 is closed as a feature |

| 79 | 08-22 09:00 | B3 (138K) | **7c06083f = M5 review candidate (items 3/4/5: commit-on-ack, ladder hold, retention+phy_reinit, VS+PE<=3)** | V2.1 fw | ANKER+Sceptre | captive | - | N | hub-backfed VBUS | 0 | USB flash → power-on + 3 v-draws | **power-on: 2 blinks, Y:22, A:0022, M:12 (new healthy form: baseline bit retained). v-draws by SCREEN-TRUTH 1/3: draw 1 latched (Y:88), draw 2 PHANTOM (C:0177, no picture), draw 3 STORM (S:04/20 = PLL unlock/PCS reset cycling, D: walking, A:0000 C:0000 Y:FF, M:02) persisting >3 min — cleared INSTANTLY by unplugging/replugging the HDMI cable at the Anker** | Candidate NOT implicated: the two failure modes are the Anker's known ones (phantom = row 78; storm = row 72 sleeping-monitor flap, env-confirmed by the HDMI-replug recovery). NEW RECOVERY RECIPE: HDMI replug downstream of the hub wakes the sink and ends the storm (no 30 s drain). Telemetry decode fixed for the record: S: = serdes_status {afull,full,pll_lock,lane_rdy[1:0],~pcs_rst,tx_run[1:0]} (S:24 normal on 138B), D: = ladder state (2E = link_established). A/B rebuild of 48576c12 from source is NOT reproducible (40 setup violations in vendor upar_arbiter @cm_life every roll; datetime stamp ⇒ non-deterministic PnR) — archive every benched bin from now on. NEXT: candidate validation on the Ugreen (sharp signature: Y:11 flat expected) |

| 80 | 08-22 am | B3 (138K) | **7c06083f M5 review candidate** | V2.1 fw | UGREEN+Sceptre | captive | - | N | hub-backfed VBUS | 0 | FULL drain (incl. HDMI unplug) → power-on + 4 watched v-draws | **power-on Y:11; draws Y:11/11/11/22, all trained (D:2E); screen: colorbars on at end, user believes 4/4 latched (attention briefly split mid-run)** | **CANDIDATE VALIDATED ON BOTH HUBS → 7c06083f = 138B CANDIDATE OF RECORD** (supersedes 48576c12, which is unreproducible — d4e5b5a1 source builds deterministically to 860d2c93 with 40 setup violations in vendor upar_arbiter @cm_life; datetime re-rolls don't perturb it). Earlier same-morning Ugreen battling (Y:88-AA pre-drain) = HUB STATE, not the build. DRAIN RECIPE REFINED (user insight): the defining element is UNPLUGGING HDMI — its +5V keeps the converter partially alive through power-only cycles; matches row 79's HDMI-replug storm recovery. M:12 = healthy steady-state on this build (baseline bit retained) |

| 81 | 08-22 am | B3 (138K) | **6b39e155 = M5 per-lane (values per lane, all-lane application) + M1: telemetry** | V2.1 fw | UGREEN+Sceptre | captive | - | N | hub-backfed VBUS | 0 | USB flash → power-on + 4 watched v-draws | **power-on Y:11, A:0022 → M:12 M1:2 (both lanes VS2/PE0 — packing verified: 0x22 = VS2/PE0 per nibble). 4/4 trained (D:2E), Y:22/11/11/33. Settled sampling: 55/55 A:0022→M:12/M1:2.** ⚠ **DRAW 2 ENDED AT M:10 M1:0 = BOTH LANES VS0/PE0 (420 mV)** — a real DRP apply fired mid-training from an **A:0000** request and committed; the link trained CLEAN (Y:11 flat) and ran at 420 mV until the next retrain | PER-LANE VERIFIED, NO REGRESSION (symmetric sink ⇒ identical to 7c06083f, as predicted; M1: field live). **TWO FINDINGS: (1) ITEM 6 ANSWERED BY ACCIDENT — the apply path works on hardware: DRP writes mid-training, commit-on-ack, truthful declaration, link still trains. (2) BUG/POLICY: an ADJUST_REQUEST of 0x00 is honored as a literal VS0/PE0 request and drops the drive to minimum. Many sinks report 0x00 as 'no adjustment needed' once locked, so a successful link can silently fall to 420 mV post-training — invisible before the items-3/4/5 RETENTION change (the old build cleared applied state at training end, so it never persisted). Harmless here (Ugreen trained clean at 420) but exactly the intermittent-failure class we chase on marginal sinks/cables. The original design comment anticipated the 0x0000 ambiguity; the chosen guard (evaluate only on adjust_de) does not cover a genuine 0x00 read.** Decision needed: honor 0x00 (DP-literal) vs treat an all-zero 0x206 as 'no request' |

| 82 | 08-22 pm | B3 (138K) | **50c4fc2c = per-lane + IGNORE_ZERO_REQUEST guard** | V2.1 fw | UGREEN+Sceptre | captive | - | N | hub-backfed VBUS | 0 | USB flash (see FLASH INCIDENT) → power-on + 4 watched v-draws | **power-on trained (D:2E, Y:22, C:0177); 4/4 draws trained; A:0022 → M:12 M1:2 on EVERY sample — no M:10 excursion (row 81's 420 mV drop) anywhere.** Y band 22/33/33/22/33 | GUARD CONFIRMED on hardware: the applied levels now stand through whatever the sink emits, VS2/804 mV held. Y band is higher than row 80's flat 11s, but the hub has NOT been drained since (row 80 established hub state drives the band, and the board has since taken a flash + power cycle) — read as hub state, NOT a build regression; a drained re-test would settle it if it ever matters. **FLASH INCIDENT (documented in jtag_flash_reliability.md): the first attempt ran under a 5-min caller-side timeout, which killed openFPGALoader mid-op and WEDGED the ESP32 USB-JTAG endpoint (LIBUSB_ERROR_OVERFLOW, IDCODE 0x555555aa); flash.sh correctly refused to retry into the wedge. Recovery = USB unplug/replug at the Mac, then re-flash — passed --verify, so nothing was left corrupt. auto_boot_1st/2nd_fail in the status dump = expected artifact of the interrupted write, not a new fault. NEW RULE: flashes run DETACHED, never on a caller-side clock** |

| 83 | 08-22 pm | B3 (138K) | **bd29a66f = phase_done status gate (IGNORE_ZERO_REQUEST now OFF) + N: instrumentation counters** | V2.1 fw | UGREEN+Sceptre | captive | - | N | hub-backfed VBUS | 0 | USB flash (detached, verify OK) → power-on (4x D4 blinks before lock) + 4 watched v-draws | **SCREEN TRUTH: power-on 4 blinks -> bars; then 6 blinks FAILED, 3 blinks OK, 1 blink OK (4th draw's outcome not observed). Telemetry said D:2E on every draw INCLUDING the failed one — another PHANTOM (trained link, no picture), same as rows 78/79.** Steady pattern every run: during training M:02 N:000, after lock M:12 M1:2 N:020 — i.e. zero_seen=0, gate_drops=2, applies=0. Counters reset per draw (virtual replug asserts phy_reinit). Y band 66/77, 66/77, 67/78, 88** | INSTRUMENTATION WORKS; three findings. **(1) NO A:0000 was observed at all this session (zero_seen=0 every run) — row 81's event is RARER than it appeared, so the zero-request question is far less pressing than the first fix implied; this is exactly the ambiguity row 82 could not resolve.** (2) The phase_done gate FIRES on this sink: 2 requests per training run arrive after the phase reports success and are dropped. ⚠ HONEST LIMIT: drop_cnt counts ANY request accompanied by phase_done, including ones equal to the applied levels that debounce anyway — so this proves the mechanism is live, NOT that it prevented a harmful apply. A sharper counter (drops that WOULD have applied) is the refinement. (3) applies=0 confirms the trust-boot-csr policy end-to-end: A:0022 == INIT ⇒ no DRP traffic at all. ⚠ Y band 66-88 is well above the post-drain 11-33 of rows 80/81, AND one draw failed outright — but the hub has NOT been drained since and the board has taken 3 flashes + power cycles; the elevated blink counts (4/6/3/1) track the elevated Y band, which is the classic un-drained-hub signature. NOT attributable to this build without a drained re-test — explicitly not claimed either way. ⚠⚠ PROCESS: row 83 was first written as 'all draws trained (D:2E)' from TELEMETRY — the exact error row 78 established a law against. D: and C: are link state, NOT picture. Classify by the screen, always. |

| 84 | 08-22 pm | B3 (138K) | **A/B: bd29a66f (phase_done gate + N: counters) vs 50c4fc2c (per-lane + zero-guard)** | V2.1 fw | UGREEN and ANKER | captive | - | N | hub-backfed VBUS | 0 | drained hub; blinks-to-lock as the metric (user's reliability predictor) | **bd29a66f: 6 blinks on UGREEN (post-drain) AND 6 blinks on ANKER — consistent across both hubs. 50c4fc2c: 1 blink (up on the 2nd) on Ugreen, same drained state.** Telemetry on bd29a66f: applies=0, zero_seen=0, drops=2 every run | **RETRACTED BY ROW 86**: 'regression' was two samples of a stochastic metric. Original claim: REGRESSION IS REAL AND REPRODUCIBLE (both hubs, post-drain, so not hub state). ⚠ EXPERIMENT DESIGN ERROR (mine): the two builds differ in BOTH logic and placement, so this A/B proves the regression but CANNOT attribute it. My stated prediction ('if the logic is innocent, 50c4fc2c should also take 6') was invalid for that reason. Note the paradox that motivates isolation: on this sink applies=0 and zero_seen=0, so the M5 changes have NO functional effect on the PHY or the AUX wire content (declared byte 0x06 both builds) — yet behavior differs sharply. Either the gate perturbs the ladder through a path not yet identified, or placement on a 0.2%-margin cm_life design dominates. **NOTE: Fmax does NOT predict it — bd29a66f had the BEST cm_life Fmax (100.382 vs 100.216) and behaves WORST.** NEXT: isolation builds, one variable each. **PROCESS: blinks-to-lock is now a REQUIRED logged field (user's best reliability predictor; audit showed it was only recorded sporadically).** ALSO: archive policy was broken — only .bin was archived but USB flashing uses .fs; recovered by rebuilding from the commit, which reproduced 50c4fc2c BIT-IDENTICAL (so PnR is deterministic for a COMMITTED source, and the earlier 48576c12 failure was an uncommitted working-tree input, not nondeterminism) |

| 85 | 08-22 pm | B3 (138K) | **PLACEMENT EXPERIMENT: 1f99bdb1 = same RTL as bd29a66f, phase_done gate STILL ON, placement perturbed via `set_option -place_option 1` (identical netlist)** | V2.1 fw | UGREEN | captive | - | N | hub-backfed VBUS | 0 | drained hub; blinks-to-lock | **COLORBARS ON THE FIRST LED LIGHTUP — ZERO BLINKS (best result of any build today)** vs bd29a66f's 6 blinks on BOTH hubs with the same RTL | 🛑 **RETRACTED BY ROW 86** (same bitstream later gave 6 blinks; this row's single-observation comparison was invalid). Original claim: **PLACEMENT IS THE VARIABLE; THE phase_done GATE IS EXONERATED.** Controlled series: 50c4fc2c (no gate) 1 blink; dbac7706 (gate present, decision OFF) 1 blink; bd29a66f (gate ON, placement C) 6 blinks; **1f99bdb1 (gate ON, placement D) 0 blinks**. The gate was already shown functionally inert on this sink (applies=0 and zero_seen=0 in BOTH gate-on and gate-off builds, identical declared bytes) — so there was never a mechanism, and the row-84 correlation was n=1 coincidence. ⚠ **Fmax DOES NOT PREDICT ACQUISITION: 1f99bdb1 has the TIGHTEST cm_life of every build (100.091 MHz) and behaves BEST; bd29a66f had the loosest (100.382) and behaved WORST.** All builds report 0 violations / TNS 0.000, so STA is blind to whatever actually varies — suspect paths that are false-pathed or otherwise unanalyzed (CDC handshakes) rather than ordinary setup paths. ⚠ n=1 for this result; repeats needed. **RETROACTIVE CAUTION: this week's build-to-build behavioral attributions (incl. rows 76/77 INIT-perturbation and row 81) each rest on single builds and are CONFOUNDED BY PLACEMENT — they need re-validation before being treated as established.** |

| 86 | 08-22 pm | B3 (138K) | **1f99bdb1 REPEATABILITY — same bitstream, 4 power cycles** | V2.1 fw | UGREEN | captive | - | N | hub-backfed VBUS | 0 | drained hub; power cycle x4, blinks-to-lock each | **0, 1, 1, 6 blinks — ON ONE UNCHANGED BITSTREAM** | 🛑 **ACQUISITION IS STOCHASTIC RUN-TO-RUN; THE MEASUREMENT IS NOT A PER-BUILD CONSTANT.** This RETRACTS row 85's 'placement is the variable' conclusion: 1f99bdb1 produced the very 6-blink result that row 85 attributed to bd29a66f's placement. bd29a66f {6,6} and 1f99bdb1 {0,1,1,6} are NOT distinguishable at these sample sizes — they may be draws from one distribution. Row 84 (gate) was already withdrawn; row 85 (placement) now falls too. **METHODOLOGY FAILURE (mine): every A/B today compared SINGLE observations of a stochastic process, and I twice built a causal story on n=1.** Consequences: (a) no build-vs-build behavioral claim from 08-22 stands; (b) blinks-to-lock needs n>=5-10 per build before any comparison; (c) the CDC audit findings remain valid as CORRECTNESS defects but are NOT established as the cause of anything observed. ALSO: `set_option -place_option 2` produces 72 violating cm_life endpoints (TNS -91.5 ns) — UNUSABLE, do not build with it |

| 87 | 08-22 pm | B3 (138K) | **e6f647aa = CDC synchronizer fixes + acquisition-counter v1 (L: field)** | V2.1 fw | UGREEN | captive | - | N | hub-backfed VBUS | 0 | flash, then 2 CALIBRATION power cycles (user counts D4 blinks, telemetry reports L:) | **Cycle 1: user saw 1 blink -> L:1. Cycle 2: user saw 0 blinks -> L:1.** Counter reads 1 in BOTH cases | **INSTRUMENT v1 BROKEN, CAUGHT BY CALIBRATION BEFORE IT PRODUCED ANY RESULT.** Root cause: v1 reset d4_cnt on `!dp_hpd`, but a failed acquisition usually drops HPD — so the reset wiped the preceding attempts and only the final successful assertion was ever counted. It could not distinguish 0 blinks from 1. **Fix (v2, build f569d4f8, timing CLEAN): FREE-RUNNING 8-bit wrapping counter, no reset at all; the reader takes DELTAS between telemetry samples (delta 1 = locked first lightup, delta N = N-1 failed attempts).** Process note: two calibration cycles cost 2 minutes and prevented an entire measurement campaign built on a broken instrument — the opposite of the n=1 failures in rows 84/85. CDC synchronizer fixes (bank pll_lock/lane_ready/FIFO flags 2FF into mgmt_clk) rode along in this build and are NOT independently evaluated |

| 88 | 08-22 pm | B3 (138K) | **f569d4f8 = CDC synchronizer fixes + acquisition counter v2 (free-running) + per-lane M5 + phase_done gate** | V2.1 fw | UGREEN | captive | - | N | hub-backfed VBUS | 0 | calibration power cycle, then 32 AUTOMATED v-draws (12 + 20) | **CALIBRATION: user 0 blinks -> L:01. AUTOMATED: 32 draws -> 30x L:01, 2x L:02, nothing worse. USER SCREEN TRUTH on the first 12: 'lit up each draw with 0 blinks and colorbars with each draw' — matches L:01 exactly. Independent cross-check: the L:02 draws are also the only ones with Y:22 instead of Y:11** | ✅ **INSTRUMENT VALIDATED — three independent agreements (user's eyes, L:, and Y:).** Blinks-to-lock is now automated: ~10 s per sample, no user labor, so distributions replace the n=1 stories that produced two wrong conclusions today. **KEY MECHANISM FOUND: a 'v' virtual replug POWER-CYCLES THE WHOLE BOARD on this bench — the free-running counter resets to 0 every draw, which it can only do if the design reconfigures. Cause: hub-backfed VBUS, so opening CC removes board power. Therefore (a) L: is read as an ABSOLUTE value per attach, not a delta, and (b) every earlier v-draw in this log was a full board power cycle.** ⚠ LIMIT: a v-draw power-cycles the BOARD but not the HUB, and the monitor stays awake — so the rare 6-blink tail seen in MANUAL cold power cycles (rows 83-86) may be hub/cold-state dependent and NOT sampled by this method. Automated draws measure the warm-attach distribution only; cold-start behavior still needs manual cycles |

| 89 | 08-24 (Mon) | B3 (138K) | f569d4f8 (CDC fixes + counter v2 + per-lane M5 + gate) | V2.1 fw | UGREEN / ANKER | captive | - | N | hub-backfed VBUS | 0 | DEAD-COLD start (boards inert ~48 h) | **Initial report: 'does not lock under 6 blinks of D4 if at all' on BOTH monitors — looked like a serious build regression. RETRACTED: the HDMI cable was still unplugged.** It is part of the power-down regimen (HDMI +5 V backpowers the hub — rows 79/80), and had not been reconnected. With no downstream sink the hub has nothing to train to, so D4 blinks without ever holding. Reconnected: colorbars up, several blinks then lock (normal cold-start range; cf. row 73 = 2 blinks, row 83 power-on = 4) | ⚠️ **BENCH LAW: a missing HDMI cable presents as an ACQUISITION FAILURE (D4 blinking without lock), NOT as an obvious 'no sink' error — it is indistinguishable at the LED from a genuine training regression.** The HDMI-unplug power-down regimen MUST be reversed before any test; check the cable first whenever a build suddenly looks catastrophic. **f569d4f8 is NOT implicated and the day's automated baseline (row 88, 30x L:01 / 2x L:02) stands.** Cold-start behavior of this build remains n=1 and unmeasured; a proper cold protocol (n>=3 per candidate, board genuinely cold) is still owed. Control build 1f99bdb1 rebuilt bit-identical and staged if a comparison is wanted |

<!-- new rows below; never reuse ids -->

## GS socket bench — in-socket 65C816 (Phase C of PRE_REV_EXERCISE_PLAN)

Own table: the DP columns above do not apply. Machine = ROM 01 IIgs, no other
cards, CPU socket empty, ribbon from the card's J5 to the socket. Card = B3
(138K) in a slot, USB-C to the Mac (serial console `/dev/cu.usbmodem5101` or `…4101` after a replug,
`+++` enters the CLI; register window `spireg 95 <idx>` / `spireg 79`).
Builds: S4 roll 1 = 20aee44d (no listen bit), listen build = af74f29a,
status-bits build = 19c79462, BE-pulse build = 4a02cd78 (§8b of
gs_socket_65816_scoping.md). Design record: docs/gs_socket_65816_scoping.md.

| id | date | build | step | reading | verdict |
|---|---|---|---|---|---|
| G1 | 09-05 | 20aee44d | first power-up, CTRL=0 | PH2 not alive, RDY low, /RES high, BE high | ⚠ INVALID: with CTRL=0 the control-input shifter (U13 port 1) is off, so PHI2 is invisible by design. Listen bit (CTRL.2) added. |
| G2 | 09-06 | af74f29a | listen (CTRL=4), machine on | PH2 alive 1.020 MHz, 142 ns low / 838 ns high (= FPI slow mode, 2/12 ticks); /RES high; **RDY low, BE low** | PHI2 receive path proven. RDY/BE low later shown to be cable mapping, not the FPI (G6). |
| G3 | 09-06 | 19c79462 | listen + slot bits | as G2 plus slot /DMA high, slot /RDY high | nothing in the slots asserting; FPI-hold hypothesis raised (wrong, see G6) |
| G4 | 09-06 | 19c79462 | arm (CTRL=5) + reset pulse via reg 0x2E 0→1 | running=1, stalls climbing, BE/RDY unchanged | core comes out of reset; RDY low stalls it; no drive because BE low. Disarmed. |
| G5 | 09-06 | — | AD3 digital in on the ribbon's IDC socket (card end unplugged) | clock on the hole numbered 7, steady high on hole 8 | ⚠ AD3 must be on a port that sources 600 mA (under-powered it opens but never acquires). Hole numbering vs the card's evidence still unresolved — see G7. |
| G6 | 09-06 | — | **AD3 directly on the motherboard socket pins 37/4/2/36** | **PHI2 1.021 MHz 86 % high; /IRQ high; RDY HIGH; BE = 66 ns LOW PULSE every cycle, fall−32 ns → fall+34 ns** | 🏆 Ground truth. RDY is not held; BE is a per-cycle FPI turnaround pulse straddling the sample edge. PHY changed: BE lows < ~110 ns ignored, receive path never gated by BE (a 4 MHz-grade 65816 drives through it). |
| G7 | 09-06 | — | cable | both ribbons built the same; continuity "DIP 4 to R38"; card counts the clock on ball AB17 = J3.28 = header pin 8 (PCB netlist + Sipeed connector table, all 38 J3 pins verified) | OPEN: either the cable is row-swapped and the card evidence is wrong, or the hand numbering on both connectors is a row off. Decider: the socket hole sitting over header pin 8 (square-pad row = odd) must carry the clock. |
| G8 | 09-08 | 4a02cd78 | new cable (ed rebuilt it), BE-pulse build flashed, listen (CTRL=4) | PH2 alive **1.020 MHz, 86 % high**; RDY high; /RES high; slot /DMA, /RDY high; BE-low counter saturated (≈7 clks/cycle) | 🏆 Card reading now identical to the motherboard ground truth (G6). G7 CLOSED: the earlier RDY-low/BE-low readings were the row-swapped cable. |
| G9 | 09-08 | 4a02cd78 | **arm (CTRL=5), reset pulsed on the GS** | running=1, **1.024 M bus cycles/s (one per PHI2), stalls 0**, status 0xFF; last-address samples all bank 0 $007D–$01BF; **ed heard the system beep on reset** | 🏆 First execution through the socket: the ROM 01 beep is software (speaker toggles), so the core fetched the reset vector, ran ROM startup and the beep routine. Display not visible (USB-C on the Mac). |
| G10 | 09-08 | 4a02cd78 | telnet console (192.168.232.127:23) tried for register reads | port 23 is a single-KEY console (c/m/p/x/g…), not a CLI: the letters of `spireg` were taken as keys and **`g` = FPGA reload from flash** — reloaded several times, socket disarmed, EQ stepped | ⚠ Register window is USB-serial only. Re-armed over USB; bench plan = arm over USB, then move the USB-C to the monitor (card is slot-powered, FPGA keeps state). Consider a telnet key that dumps the GS window. |
| G11 | 09-08 | 4a02cd78 | armed over USB, USB-C moved to the monitor, reset pulsed | **ROM 01 splash screen: "Apple IIgs / Copyright Apple Computer, Inc. 1977-1987 / All Rights Reserved. / ROM Version 01"** on the card's own DP output | 🏆 Drop-in goal met: the FPGA core boots the machine through the socket; the card's video capture works while its own core drives the bus. Next rungs: Check-startup-device / boot, Control-Reset to Applesoft, typed input. |
| G12 | 09-08 | 4a02cd78 | Control-Reset at the splash, several times; Apple-Control-Esc | beep + splash redrawn every time; **never "Check startup device"**; Control Panel does not open | ROM 01 reset handler (disassembled from 342-0077-b): cold path writes $3F2=$E000/$3F4=$45 then enters the slot scan; any later reset with that check passing jumps to Applesoft. Cold every time + no slot-scan message ⇒ the core is stuck in the cold-start code BEFORE the slot scan (ed's hypothesis: where ROM 01 switches the FPI to 2.8 MHz from battery RAM). PHI2 period was never re-read after the reset pulse — next reading answers it. |
| G13 | 09-08 | fw + trace build | telnet `:` line mode (ESP32 fw flashed over USB with the GS off) | `gs` returns the decoded window in one shot over WiFi; `spireg` works | Monitoring no longer needs the USB cable. Trace ring (CTRL.3 freeze, regs 23–28) building for the next power-up. |
| G14 | 09-08 | trace build 58b0edc5 | armed over telnet with the machine at the splash (arming with /RES high = cold start); `gs trace` | PHI2 still **1.020 MHz** (never switched to fast); 64 cycles = bank 0 **$00C4,$00C5,$00C6,$00C7 data $FF, then $00B3, then $00C8…** (first build had the entry bytes mis-mapped in the register mux; decoded by hand) | 🏆 The core is **executing opcode $FF out of zero page**: SBC long,X = 4 bytes of $FF + one data read at $FFFFFF+X → wraps to $0000B3 (X=$B4). Five cycles per instruction marching up ZP = a PC runaway, not a wait loop. Not the speed switch. Next: trigger build (freeze 32 cycles after the first opcode fetch from bank 0 < $0800, VPA/VDA per entry) to see the bad pull/vector. |
| G15 | 09-08 | trig build 78d9b0f7 | armed with the trigger enabled (CTRL=0x15) | trigger fired on the core's own RESET sequence ($0000 with VPA&VDA) — but the capture is a textbook cold start: vector $FFFC/D → $FA62; bytes A9 01 0C 29 C0 A9 FB 78 D8 1B 20 36 FE match the ROM image; JSR pushes FA/6E to $01FB/$01FA; internal cycles on 1-byte ops; TSB $C029 read-modify-write | 🏆 Reads, writes, bank FF (data C0 at FF:FFFF vs 00 at 00:FFFF), stack and R-M-W all correct through the socket at the start. Trigger held off until the first ROM opcode fetch in the next build. |
| G16 | 09-08 | trig build 78d9b0f7 | software restart (CTRL 0x04→0x15) with the trigger re-armed 0.6 s, then ~ms later | every capture already in the runaway: $FF from ZP, one BRK frame per 64 KB lap (SP $01B5→$0164), $C0xx executed as code scrambles soft switches (LC RAM at $FFFE, speed fast→slow) | The PC leaves the ROM within the first few ms of a cold start. Post-runaway state is chaos, not the cause. Next: reset-proof trigger (trig_ready after the first ROM opcode fetch) = build 3; sim of the same ROM image with ideal memory running in parallel. |
| G17 | 09-08 | build 4 7683532b (trigger held off until the first ROM opcode fetch; CTRL.5 force-slow) | CTRL=0x15 at power-on | trigger fired on a LEGITIMATE bank-0 opcode fetch: ROM 01 pushes `54 E0 FE` (MVN $FE→$E0) plus an RTL frame onto the stack and executes it from $0193, copying FE:0EFD… to E0:1E00… — correct data, the core still in ROM afterwards; PHI2 averaged 2.5 MHz at that moment | The core gets well past the speed switch; stack-resident code is normal ROM 01 behaviour, so the trigger must exclude the stack page. Sim never reaches this copy (its path branches differently on battery-RAM/clock/slot reads) — sim is not a reference past the first few hundred cycles. |
| G18 | 09-08 | build 4 | restart with CTRL=0x25 (force-slow: bit 7 cleared on writes to $C036) | PHI2 1.020 MHz throughout; **runaway anyway** — same $FF-from-zero-page pattern, BRK frame per lap | ⚠ Fast mode is NOT the cause (my earlier claim withdrawn): the failure occurs at 1 MHz too, within ms of the start. Next: trigger v3 = first opcode fetch from bank 0 RAM outside the stack page, 56 cycles of history (build 5). |
| G19 | 09-08 | build 5 6325fdd9 (trigger v3) | power-on, CTRL=0x15 | 🏆 **ENTRY CAPTURED**: … JSR $F9DA; SEI; LDA #$10; TSB $C027 (read 80, write 90) — then the fetch at **$F9E0 returns 09 90 03 8D 10 C0 22 4C 00 E1…, which are the ROM bytes at $B9E0**; $F9EA→$B9EA likewise; the BRK vector read at $FFFE returns A9 00 = ROM $BFFE. Bank E1 reads (E1:004C = JML $FFB5DE) and bank-0 RAM reads ($BAAF = 00 00) are right. | **A14 is low at the ROM from the $C027 write onward** while the core, the PHY address register and every other line are correct; the FPI still decodes the region as ROM. TSB #$10 on $C027 enables the ADB data interrupt → /IRQ goes low and stays low (never serviced) → suspect A14 shorted/coupled to /IRQ between FPGA and socket. |
| G20 | 09-08 | — | ⚠ software "restarts" (CTRL 0x04→0x15) | cycle counter never reset, no trigger fired | Disarm/re-arm does NOT restart the core; G16 and G18 were the same runaway observed again. G18's "runs away at 1 MHz too" is VOID — redo force-slow with a real reset (CTRL=0x25 then Control-Reset). |
| G21 | 09-08 | — | ROM 03 source listing (`Monitor.aii.lst`, ENBDATAREG) + ROM 01 image | the instruction before the divergence is `TSB KMSTATUS` ($C027); the core's R-M-W internal cycle read **$C028 = ROMBANK**; the FPI honours it (no VDA/VPA at the socket) and swaps the bank-0 ROM window | 🏆 **ROOT CAUSE.** Not electrical, not fast mode, not RDY: a phantom I/O access on an internal cycle. Fix = PHY repeats the previous address/bank on internal cycles aimed at I/O space (scoping doc §4.5); bench asserts it (TSB $C029 at the ROM start exercises it) and counts $C028 accesses. Build 6 in progress. |
| G22 | 09-08 | **build 6 0eae1a71** (internal-cycle fix) | power-on, CTRL=0x15 | PHI2 sustained **2.7–2.8 MHz**, last addresses in bank FF ROM for minutes, stalls 0; the trigger fired only on the slot-6 boot firmware's `JSR $0000` idiom (RTS planted at $0000 to find its slot) after the startup slot scan (`JMP ($0000)` → $C600) | 🏆 **FIX CONFIRMED ON HARDWARE**: the cold start completes and the machine runs its boot scan at full speed. Screen / Control-Reset / keyboard checks next. |
| G23 | 09-08 | build 6 0eae1a71 | screen after the cold start | splash, then the monitor: `00/0801: 00 00 BRK 00  A=FF00 X=0060 Y=0000 S=01F8 D=0000 P=72 B=00 K=00 M=0C Q=88 L=1 m=1 x=1 e=1 *` | 🏆 Full boot ladder to "needs a disk": slot scan → the a2mega's own slot-6 block device (X=$60) → boot block 0 read (all zeros, no image mounted on 1.0a3) → BRK at $0801 → monitor with a correct register dump. Next: keyboard at the `*` prompt, Control-Reset → Applesoft, then mount a .po via the card's FTP server and boot ProDOS. |
| G24 | 09-08 | build 6 0eae1a71 | Control-Reset at the monitor, then `print 2+2` | Applesoft `]` prompt; typed text echoes; prints `4` | 🏆 **Interactive machine through the socket**: ROM, ADB keyboard ($C000/$C010), Applesoft, text video, all at 2.8 MHz. Drop-in goal met. Remaining rung: the a2mega's own block device returned a zero block 0 to this CPU although `4soniq_v2.po` is mounted (works with the real 65816) — HDD ROM polls busy with `ROL hd_status,X` (R-M-W) and the card fires EXECUTE/auto-increment on address-level touch edges; address-match trigger (build 7) to capture the boot-block copy at $0801. |
| G25 | 09-08 | build 6 | `PR#6` from the Applesoft prompt (machine at 1 MHz: Control Panel speed is Normal; the 2.8 MHz seen earlier is ROM 01's startup-only fast phase) | screen prints a row of flashing `+`; trace = core spinning in the card firmware's busy poll (`ROL $C0E1,X` / `BCS`), status reads $80 for ever; FPGA regs 0x26=0x01 (HDD unit 0 READ pending, LBA 0) never acknowledged | At 1 MHz the EXECUTE touch fires and the FPGA raises the request, but the ESP32 disk task never services it (polls every 2 ms; nothing gates it in the source). Card-side, not the socket. Note: this ROM 01 machine has never booted ProDOS from the a2mega's own block device (only from an a2n20). |
| G26 | 09-08 | build 6 | fast cold boot (G23) vs G25 | cold boot: zero block, busy never seen; PR#6 at 1 MHz: busy stuck | Two distinct card-side behaviours; the fast-mode one is consistent with the card's address-level touch edges not occurring between consecutive slot accesses at 2.8 MHz (slots see only synchronized cycles). To be pursued as an a2mega block-device item, separately from the socket work. |
| G27 | 09-08 | build 7 da070cfa (address trigger) | power-on, CTRL=0x55 with trigger address 00:0801 | capture of the boot-block copy tail: `LDA $C0E8,X` reads $00 every time, `STA ($44),Y` to $09FE/$09FF, `ROR $C0E1,X` → no error, `BIT $C061`, `JMP $0801` → BRK → vector $FFFE = 74 C0 (correct ROM bytes) → $C074 | Fast-mode case confirmed: transfer logic runs correctly, the card's sector buffer was never filled (EXECUTE touch not seen at 2.8 MHz). The ROMBANK fix is also visible here (vector now correct). |
| G28 | 09-08 | build 7 | FTP RETR of the mounted image; `PR#6` again after an ESP32 restart | **blocks 0 and 1 of `4soniq_v2.po` are all zeros** (no boot loader); PR#6 at 1 MHz with a fresh ESP32: copy loop reads $00, request register clears (served) | ⚠ The "zero block" was the IMAGE, not the card or the CPU: the block device served block 0 faithfully and the machine did what a real one does with a non-bootable volume. G26/G27's "EXECUTE touch not seen at 2.8 MHz" withdrawn. Only the pre-restart stuck request (G25) remains as a card-side oddity. Next: a bootable image (`mb-audit-v1.60.po` uploaded as hdd1.po). |
| G29 | 09-08 | build 7 da070cfa | `hdd1.po` = mb-audit-v1.60 uploaded over FTP; ed unmounted `4soniq_v2.po` from the card's menu (the settings entry otherwise wins over the `hdd1.*` candidates); armed | **ProDOS boots from the a2mega block device and mb-audit runs** (ed: "it just booted hdd1.po") | 🏆 **Boot ladder complete**: reset vector → ROM 01 cold start → slot scan → block device → ProDOS 8 → application, all through the socket with the FPGA 65816. The drop-in iteration's goal (electrically correct replacement) is met. |
| G30 | 09-09 | fw a705e563 (S6 auto-arm) + build 7 | full card power cycle, no console | **booted on its own** into ProDOS/mb-audit; `gs`: CTRL=0x05, AUTO: ARMED (PHI2 ALIVE), 2.7 MHz | 🏆 Auto-arm works. Log order was LISTENING → HDD1.PO MOUNTED → A2: RESET RELEASED → PHI2 ALIVE - ARMED: the release beat the arm by tens of ms (fast LittleFS mount) — harmless (arming with /RES high cold-starts the core) but the release should wait for the auto-arm to settle; fix follows. |
| G31 | 09-09 | fw 641b209c (release gated on auto-arm) | card USB-powered FIRST, then GS on; GS off/on again | auto-arm log: LISTENING → (release after 300 ms no PHI2, GS still off) → PHI2 ALIVE - ARMED → PHI2 LOST - RELEASED → PHI2 ALIVE - ARMED (state machine correct through the cycle). **But the machine never leaves reset**: socket /RES = 0, FPGA status reg 0x07 bit 2 (slot RESET) = 0, 0x2E = 1, hold backstop long expired, FPGA reload (telnet g) did not clear it; no chime, twice | ⚠ Reproducible **card-powered-before-machine** failure — the machine stays in reset. FPGA hold (2G06 via a2_res_out_n) is released by its own logic; suspects: LSF0108 bidirectional /RESET channel state after the 5 V side powers up late, or the IIgs power-on reset not firing on a pre-charged 5 V rail (backfeed). To measure: J5-15 / slot 5 V with GS off + card on USB; /RESET at slot vs 2G06 output with GS on. Normal order (card unpowered → GS on) boots every time (G30). |
| G32 | 09-09 | fw 641b209c | (a) stuck machine: `gs listen` (socket undriven, input shifter on) | **/RES rose at once** (STATUS res_n=1, reg 0x07 bit 2 = 1); re-arm → core running | 🏆 The holder was OUR SOCKET DRIVE: arming while the IIgs is still in its power-on reset keeps that reset asserted; release the socket and the power-on sequence completes; arming afterwards is fine (Control-Reset while armed works, G24). Rule: arm only when PHI2 alive AND /RES high. |
| G33 | 09-09 | fw 641b209c | (b) ed: USB unplugged, slot-powered start | auto-arms, boots, then a reset a few seconds after boot, boots again | FPGA hold quirk exposed by the early socket writes: `mcu_absent` (no STATUS read within 3 s of config) drops the hold → boot; the first STATUS read later re-asserts it → reset; mount-complete release → boot 2. Fixes: fw reads STATUS before touching the socket; gateware makes any release sticky (build 8). |
| G34 | 09-09 | fw 80e28194 (/RES-gated arm) | card on USB first, GS on: (a) listen only (CTRL=4, never armed) → slot RESET low (0x07 bit 2 = 0), no beep; (b) CTRL=0 (socket shifters fully off), GS power-cycled → slot RESET HIGH (0x07 = 0x07): machine completes its power-on reset (no beep only because nothing is armed) | **Enabling U13 (control-input shifter, port 1 B→A, 1DIR = GND, VCCB = card +5 V) across the IIgs power-up is enough to keep the machine in reset**; with everything off the machine comes up normally. Mechanism not understood (an input port should not be able to hold /RES). Firmware rule: keep the socket shifters disabled until the slot side shows the machine out of reset, then listen, then arm on PHI2 alive + /RES high; adopt an already-armed FPGA on ESP32 restart. |
| G35 | 09-09 | gateware build 9 4c19b070 (STATUS.7 alive, 0x2E.1 assert; ⚠ 1 setup violation −5 ps on the DP pixel CDC FIFO, not socket logic; build 10 same source: 9 AUX-ladder violations — 138B knife-edge, no roll-hunting) + fw v2 | sampler across a power-on with the card powered first: clock alive at 10.7 s, **slot RESET never rose** with the socket fully off and our hold released. Then: our hold asserted (0x2E=3) BEFORE the machine powered up → machine on → release (0x2E=1) → **RESET rose at once** (0x87) → firmware armed → booted at 2.8 MHz | 🏆 **The machine must power up under our reset.** With the card powered first and nothing holding the slot reset, this IIgs's own power-on reset never completes (G34's one success was luck); holding it through the ramp and releasing afterwards works every time so far. fw v3: MACHINE OFF = socket off + hold asserted; clock alive → listen → PHI2 → arm → release; no PHI2 within 2 s → release and idle (plain card). |
| G36 | 09-09 | fw v3 (hold through power-up, listen+arm under the hold, release) | hands-off power-on, card powered first | log: MACHINE ALIVE - LISTENING UNDER OUR RESET → PHI2 ALIVE - ARMED, RESET RELEASED; but slot RESET stayed LOW after the release (0x83, hold=0), core held, no beep | The machine's **first** reset release after power-up must happen with the socket shifters OFF (G35's manual success released with the socket off, then re-asserted/armed/released); later resets with the socket enabled are fine (G24, G35 second cycle). fw v4 adds that step: alive → release (socket off) → reset rises → assert → listen → PHI2 → arm → release. |
| G37 | 09-09 | fw v4 | hands-off power-on (card first): v4 released 100 ms after the clock appeared, socket off → slot RESET never rose (2 s), retried every 2 s (sampler gswatch4); manual: 5 s hold then release → still no rise (machine wedged); **power cycle with the hold asserted, release ~10 s after the clock (socket off) → RESET rose (0x87)**; `gs auto` → v4 re-asserted, listened, armed, released → **beep and boot** | 🏆 Timing of the FIRST release matters: released too early (100 ms after the clock) the IIgs's own reset logic wedges until a power cycle; several seconds of hold works. fw v5: 5 s hold after the clock before the first release (socket off), then the assert/listen/arm/release cycle. |
| G38 | 09-09 | **fw v5 7aa4a571 + gateware build 9 4c19b070** | hands-off: card on USB, GS off → on (sampler gswatch5) | t=32.1 clock stops → 32.6 hold asserted, CTRL=0 → 34.4 clock back (under hold) → 39.5 release, RESET high at once (0x87) → 39.7 armed (CTRL=05, GS STATUS FF) → 41.6 HDD request in flight → **beep and boot** | 🏆 **Auto-arm works unattended in the hard order** (card powered before the machine) and through a machine power cycle with the card kept powered. Remaining: slot-powered order (USB unplugged). Gateware note: build 9 has one −5 ps DP pixel-FIFO setup violation (not socket logic); a clean roll of the same source is still owed for the merge record. |
| G39 | 09-09 | fw v5 (hold tunable) | bisecting the post-clock hold: 1000 ms warm → boot (clock→armed 1.1 s); 500 ms: warm → boot, **cold (12 s off) → wedged**; 2000 ms cold: first release OK (reset rose), then the assert/listen/arm/release cycle ~100 ms later → **/RESET stayed low** (sampler gswatch6-8) | The first release needs ≥~1–2 s after the clock on a cold machine, and a **second reset pulse right after it wedges a cold machine**. Once wedged only a power cycle recovers (hold/release retries do not). |
| G40 | 09-09 | **fw v6 3ec01a1f** (single reset: timed first release, then listen+arm with /RESET high; default hold 2 s) | cold start (23 s off), card on USB, hands off (gswatch9) | clock 87.84 → release 89.84 → armed with reset high 90.08 (CTRL 05, GS FF) → block read 91.67 → boot | 🏆 One reset, 2.2 s from clock to running core, cold start. This is the sequence to ship: hold while dead → clock + 2 s → release (socket off) → reset high → listen → PHI2 → arm. |
| G41 | 09-10 | gateware build 11 (POR probe, 0/0) + fw v7 | hands-off, card on USB: probe releases at the machine's natural reset end (≈230 ms after its clock) and hardware-arms the socket in the same clock | armed at +230 ms → machine asserts /RES again and stays (no beep), nothing of ours holding | Early CPU start (≈230 ms after the clock) wedges the machine, even with the CPU present at the exact release. The probe itself works (por_done). |
| G42 | 09-10 | same + arm-delay knob | probe release at +230 ms with the socket OFF, arm 1.5 s later | /RES stayed high through the headless 1.5 s; core started, then ran away into bank E1 ($FF opcodes) at 1.02 MHz, no beep | Early release is harmless; a late CPU start (machine headless 1.5 s) does not boot cleanly either. Working window so far: CPU start ≈1–2 s after the clock, right after our release. A real 65816 starts at ≈230 ms and boots ⇒ something our core does in the first second, that the real chip does not, trips the machine's reset — same class as the ROMBANK bug. Next: trace trigger on the socket /RES falling (TADDR = FF:FFFF, build 12) to see the last 56 cycles before the machine resets. |
| G43 | 09-10 | gateware build 12 68535493 (/RES-fall trigger, 0/0) + fw v7 | hands-off, card on USB, machine off ≈25 min before: probe + hardware auto-arm at the natural release | **beep and boot** at the natural release (CTRL 05 by the FPGA the moment the machine let go, reset stayed high) | Same sequence as G41 (which wedged) → the early-start wedge is INTERMITTENT; only visible difference: time powered off before the cycle (≈1 min in G41 vs ≈25 min here). Trigger did not persist (firmware's explicit CTRL write cleared bits 4/6 — fixed). Next: repeated power cycles with the /RES-fall trigger persistent until a wedge is captured. |
| G44 | 09-10 | build 12 + fw a7842a96 (trigger bits persistent) | second natural-release power cycle with the /RES-fall trigger | booted (beep) but the trigger fired: trace = ROM in the **slot 5 Disk II boot loop** (`LDA $C08C,X`/`BPL` on $C0DC — the HDD card had answered STATUS 'not ready', so the boot fell back to the floppy), then the last address repeated for **4 cycles and the reset vector sequence** (a ~1.5 µs /RES pulse at the socket), then a restart that ended in zero page at 2.8 MHz | Two card-side facts to explain, both deterministic in nature: (1) the HDD unit reports not-ready to a CPU that starts at the machine's natural release but ready to one that starts ≥1 s later; (2) a microsecond reset pulse reached the socket while the ROM polled the Disk II register. **Stopped here for the night.** |
| — | 09-10 | STATE AT END OF DAY | card: gateware build 12 68535493 (POR probe, /RES-fall trigger), fw a7842a96 (v7: hold while dead → clock → FPGA probe releases at the machine's natural reset end and hardware-arms; `gs hold <ms>` = optional arm delay after that release; trigger bits kept) | natural-release arm: 2 boots / 1 wedge / 1 pulse-restart today; fixed 1–2 s hold + arm (v6 behaviour) 4/4 boots | Ed's position (adopted): this is a **deterministic sequencing problem, not a timing window** — CPU bring-up is a fixed sequence, the TransWarp GS does it, we control the reset release fully, and this card already coexists with a TransWarp-equipped GS. The measured 'windows' are symptoms. Next experiments (deterministic): (a) trace the HDD `ready` path from the ESP32 flags through `hdd.sv` `volumes[].ready`/slotmaker across an Apple reset — which state is lost when the machine's own reset rises before/after ours; (b) locate the µs reset pulse — socket (`gs_res_n`) vs slot (`a2_reset_n`) with a second trigger and FPGA timestamps; (c) log the ROM's first slot-scan outcome per start type; (d) reference the TransWarp GS bring-up (it must hold/sequence the 65816 socket the same way). Interim for a reliable bench: `gs hold 1000` after a power cycle or v6 semantics. |
| — | 09-10 | ed's lead on the G44 pulse | the ~1.5 µs /RES pulse almost certainly came from the card via the slot interface: FPGA_RES_OUT (the 2G06 hold path) or a transition of FPGA_BUS_OE, which has been observed to strobe the open-collector lines through the LSF0108 when it changes state | Checked: `a2_bus_oe` is tied to constant 0 in top.sv (never changes at runtime), so the strobe would need another changing line (a2_a_dir/a2_d_dir direction pins, LSF EN) or our RES_OUT itself. Note the probe also releases/re-asserts RES_OUT every 1 ms for 40 µs *before* por_done — if the machine's power-on reset is an RC rather than an actively driven low, those windows glitch /RESET high during its own POR (a deterministic disturbance the fixed-hold sequence never produces). Next instrument (gateware): timestamp (sys_time) every reset_hold edge, every /RES fall at the slot (a2_reset_n) and at the socket (gs_res_n), and every 0x2E / GS CTRL write, into a small event FIFO readable over the window — the pulse's source and the firmware's writes then line up on one timeline. |
| — | 09-10 | ed, precise version | **Three known levers on the slot RESET line:** (1) FPGA_RES_OUT (2G06, driven by `reset_hold`) — the mechanism meant to control reset even before FPGA_BUS_OE is enabled; (2) FPGA_RES — the bidirectional /RESET channel through the LSF0108 (FPGA pin `a2_reset_n`, an input with pull-up in the design); (3) FPGA_BUS_OE — asserted once shortly after configuration (`a2_bus_oe` = constant 0 in top.sv); **enabling the LSF0108 through it has been observed to momentarily drive the open-collector control lines to ground** despite the FPGA-side pull-ups, contrary to TI's datasheet, and the belief that the pull-ups mitigate it may be wrong. Its timing relative to FPGA_RES_OUT at configuration is unknown. | For the G44 pulse (no configuration event) the candidates are (1) and (2); for the power-up sequence all three matter. The event-timestamp instrument must therefore also record: configuration → first `a2_bus_oe` assertion (LSF enable) and the `reset_hold` level at that instant, plus the `a2_res_out_n` pin's pre-configuration pull state (a 'break' in the hold across configuration if it is pulled low). |
| — | 09-10 | pin facts (cst) | `a2_res_out_n` F21 PULL_MODE=UP; `a2_bus_oe` U21 PULL_MODE=UP; `a2_reset_n` G22 PULL_MODE=UP | Before/during configuration the 2G06 input is pulled high → RESET asserted; the loaded design keeps `reset_hold`=1 → **no break in the hold across configuration**. BUS_OE is high (transceivers + LSF0108 disabled) until the design drives it 0 → the LSF enable strobe happens **while our hold is asserted**, so /RESET is masked then but the other open-collector lines (INH/IRQ/RDY/DMA/NMI) are strobed. Neither lever changes state in a running system, so the G44 pulse needs RES_OUT (a `reset_hold` glitch) or the LSF channel (FPGA_RES) — the timestamp FIFO decides. |
| — | 09-10 | ed, IIgs schematics (`bit-preserve/Apple/Apple IIGS/IIgs`, KiCad) | the IIgs RESET line is **driven by the Mega II as a power-on reset**, not an RC | The probe's 40 µs windows cannot glitch an actively driven low; a high sample really is the Mega II releasing. The Mega II is now also a candidate for the G44 µs pulse and for the early-start re-assertion (what makes the Mega II assert reset again after its POR?). Next: enumerate every driver on the RESET net from the schematic (Mega II, KEYGLU/ADB micro, slot pin 31, CPU pin 40) before the next experiment. |
| — | 09-10 | references: `bit-preserve/.../IIgs/Apple_IIgs_Symbols.kicad_sym`, `/Users/edanuff/GitHub/Mega-IIe` (ed: the best Mega II analysis — a working Apple built around an extracted Mega II) | symbol library: every RESET pin is declared `input` (convention), but **M50741 (ADB keyboard micro) has `~RESET_IN` and `~RESET_OUT`**, the Mega II's `~RESET` is an input | Open discrepancy to settle from the schematic net (ROM 3 sheets; bench machine is ROM 01): is the IIgs power-on reset sourced by the Mega II (ed's research) or by the M50741's RESET_OUT (classic arrangement: keyboard micro drives power-on and Control-Reset)? Either way the driver is active logic, not an RC; whichever it is, its post-release behaviour (re-assertion conditions) is the thing to read before the next experiment. In Mega-IIe rev3b the ~RESET net reaches the Mega II, CPU, IWM, slots and the RP2040 keyboard sheet (the RP2040 plays the keyboard-micro role). |
| — | 09-10 | ed, resolved | the **M50740** (Mitsubishi 8-bit MCU; ADB + keyboard controller on early/ROM 01 IIgs boards; M50741 on later boards) **can pull /RESET low** — it is the machine's power-on reset source and can re-assert reset later | Deterministic-sequence hypothesis for the early-start failures: the ROM's first work after reset is the KEYGLU/ADB exchange ($C026/$C027: ENBDATAREG, sync/mode commands); an early-started CPU whose bus behaviour on those registers differs from the real 65816 (our only confirmed bug so far was exactly there — the $C027 internal cycle) can make the micro assert reset and hold. A CPU started ≥1 s later meets a micro that has finished its own init, masking the difference. Next: (1) trace with the /RES-fall trigger plus an address trigger on the first $C026/$C027 access at an early start, compare our cycle-by-cycle sequence on those registers with the datasheet 65816 (R-M-W double reads, data hold on the $C026 write, IRQ vector pull after CLI); (2) the M50740 firmware's reset conditions (Mega-IIe RP2040 keyboard emulator reproduces the protocol — read it); (3) the timestamped event FIFO. |
| — | 09-10 | Applefritter "IIgs not resetting" (ed) | robespierre: the M50740 is "the only chip with its own power-on-reset timing circuit"; after it leaves its own reset it runs a self-test/sanity check, possibly talking to the ADB GLU, then **pulls P25 (pin 49) high to bring the rest of the computer out of reset**. ggb: the **Mega II pulls RESET low until its internal power-on circuit considers power stable, and resets itself internally whenever RESET is pulled low externally**. Thread's fault: a lifted pin 49 → machine never left reset (sawtooth on /RES). | Two drivers at power-up: Mega II (POR, low) and M50740 (release = its init complete). So the natural release we probe is the micro saying 'initialized'. And every external assertion (our hold, our probe's re-asserts, the FPGA's storage hold) restarts the Mega II's internal reset sequence. To read against the early-start data: what the micro/Mega II do in the first ~1 s after P25 rises, and what the ROM's first KEYGLU/ADB exchange expects — with the M50740 driving the line high while our 2G06 holds it low, there is also contention during our hold that the real machine never sees. |
| — | 09-10 | **TransWarp GS schematic** (ReActiveMicro release 1, Geoff Body, 2016; read as 400 dpi crops + `pdftotext -bbox`) | how the TWGS brings its 65816 — and the CPU socket — out of reset | (1) **CPU_RESET = GS_RESET — the CPU SOCKET's own /RES (J5 'To CPU cable' pin 2 = DIP pin 40), not the slot's — re-registered by one 74F74** (U35B: D = GS_RESET, CLK = /CPU_PH2, PRE = +5 V, CLR = the XC2064 FPGA's DONE pin, Q pulled up 10 k): the CPU sees the machine's own reset one CPU clock later, synchronous to the CPU clock, and is held in reset until the card's FPGA is configured — no RC, no timed hold, no re-assert, nothing enabled at release time. (2) CPU_RESET drives the TWGS's own W65C816 RESB and the on-card header J1 (which also carries the fast CPU's VPB/VDA/VPA/M-X/E/MLB and SEL_GS_PH2 — it is not the socket cable; corrected 09-11 after ed's read, the first version of this row had J1 and J5 confused). (3) The socket cable J5 uses the standard IDC→DIP mapping (pin 15 = +5 V = DIP 8 VDD) and leaves socket pins **3 RDY, 4 VDA, 6 M/X, 9 MLB, 12 E, 13 VPA unconnected**; VP, /RES, ABORT, IRQ, PH2, BE, NMI, R/W and the buses go through. The slot edge uses only pins 18 R/W (trace-cut), 36 7M, 21 RDY, 22 DMA, 40 PH0, 37 Q3, 28/23 INT and 27/24 DMA daisy-chain, 25 +5 V, 26 GND — **slot pin 31 /RESET is not used at all**. Slot 7M/RDY/DMA/PH0/Q3 feed GAL U44 (clocked by 7M; PH0 and Q3 through inverters + 100 Ω/100 pF), whose outputs (RW_TO_GS, DMA_ADDRESS_IN, and through U27C) make **GS_PAUSE**, steering the U22 74F157 mux and the U23 74F175 — the fast CPU's wait mechanism for slot RDY/DMA. (4) CPU_BE comes from the TWGS DMA logic (U25B ← slot /DMA through GAL U44 `DMA_ADDRESS_IN`); the 74LS245/74AC245 bus buffers get DIR/G from logic and are never switched as part of reset. (5) The TWGS can pull GS_RESET low itself: Q1 2N3904 open collector, base through R16 5.1 k from the card logic — the same open-drain role as our 74LVC2G06. (6) XC2064 M0 = CPU_RESET (configuration mode pin while reset is low); DONE is also re-registered by U33B (X4_EARLY) and OR'ed with CPU_RESET for the internal state machines. | **The TWGS model: reset to the socket CPU = the machine's own /RESET, synchronised to the CPU clock, gated only by "my card is configured"; the bus buffers are up from configuration on.** The reset it follows is the socket's own pin 40 — electrically the motherboard RESET.L that also reaches slot pin 31 (next row) — and it only reads that pin, never drives it. Both TWGS manuals on /Volumes/Storage/Downloads are scanned images (pdftotext empty); TWGS ROM source at `/Users/edanuff/GitHub/TransWarpGS-ROM`. |
| — | 09-10 | **IIgs ROM 3 KiCad netlist** (`/Applications/KiCad/KiCad.app/Contents/MacOS/kicad-cli sch export netlist --format kicadxml`; scratchpad `iigs_rom3.xml`, `iigs_nets.py`) | every node on RESET.L and RDY.H | RESET.L (18 nodes): slot pin 31 ×7, **CPU U6 pin 40 directly**, FPI (CYA UF4 pin 23), Mega II UI2 pin 35, **M50741 UG12 pin 49 P25** (its reset output), KEYGLU UI12 33, VGC UF2 47, IWM UC5 23, Slotmaker UC10 2, DOC UJ12 20, SOUNDGLU UJ7 1, UD12 74LS74 pin 2 (D). **No discrete pull-up on the net** (pulled inside the Mega II / M50741). M50741 ~RESET_IN (pin 16) = its own POR RC: **R105 200 k + C39 1 µF + CR2 1N914 (τ ≈ 200 ms)** — matches the ≈230 ms natural release the probe measured (G41–G44). M50741 ~RESET_OUT pin 28 is unconnected. RDY.H (12 nodes): slot pin 21 ×7, **CPU pin 2 RDY**, R113 4.7 k pull-up, FPI pin 55 INVBADR.L, UD12 74LS74 pins 12/13 (D, /R; UD12 /Q6 = DRES.H → IWM drive-reset logic). CPU BE pin 36 = BE816-BED, R133 100 Ω from the FPI's BE816-BE; CPU PH2 pin 26 = PH2D.H via R195 22 Ω (shared with the FPI PH2 input and the memory connector); CPU ABORT pin 3 = memory-connector pin 32 + R134 4.7 k. | Numbers for the deterministic model: the machine's natural reset end = the M50741's ~200 ms RC + its self-test, then P25 high; the CPU RESB pin IS the slot /RESET net, so anything our socket side does to that pin is seen by the Mega II (which re-resets on any external low). ROM 3 board — confirm on the ROM 01 bench machine with a meter (CPU pin 40 ↔ slot pin 31). |
| — | 09-10 | proposal from the TWGS model (not yet tried) | next experiment | Arm like the TWGS: (1) hold slot /RESET ourselves while the machine is dead → clock appears; (2) **enable the socket shifters and the core (arm) while /RESET is still low** — the core then sits in reset exactly as a 65816 would (address bus driven, R/W high, data hi-Z); (3) release our hold no earlier than the machine's own release would have come (probe/por_done, ≈230 ms) and let the core leave reset on the /RES rise, synchronised to PHI2 in the PHY — nothing is switched at release time. Added latency = the M50741's own ~200 ms, inside the 500 ms budget, and it removes v7's "arm in the same clock as the release". Reconcile with G34/G36 first (arming across the machine's power-up kept it in reset): those were with the machine unclocked/ramping; here the machine is clocked and only held by us. Run it with the /RES-fall trigger armed and watch ed's two remaining levers (FPGA_RES_OUT, FPGA_RES). |
| — | 09-11 | decision record (ed) | RDY: listen only, never drive — written up as scoping §3.3 "Deliberate departures from the W65C816 datasheet" | Datasheet: RDY is bidirectional (input halts at the PHI2 fall; output pulled low by the CPU during WAI). We sample it at the fall and repeat the cycle while low; RDY_OUT (U15 open-drain) stays released; the slot RDY driver stays idle too. Evidence: (1) IIgs netlist — CPU RDY pin 2 = slot RDY.H (4.7 k, FPI pin 55, UD12 flop), no consumer of a WAI announcement, protocol tied to the 1 MHz PH0/Q3 cycle, honoured only at Normal speed; (2) the TransWarp GS leaves the socket RDY pin open on its cable, pulls its own CPU's RDY up and never uses it, and turns slot RDY/DMA into GS_PAUSE = a clock hold through GAL U44 (7M/PH0/Q3) and the U22 74F157 mux. | Ed: this is the correct call, and it must be findable later without re-deriving it — hence §3.3 (also lists the BE pulse, the internal-cycle address repeat and VP as the other intentional departures). Revisit only if the core ever runs faster than PHI2 (then the TWGS clock-hold scheme applies) or if software is found that depends on seeing WAI on the RDY net (none known). |
| — | 09-11 | **gateware build 13 = `a2mega_138B_s6evt.fs` sha256 fd84b885ef8f88b5cb0bfe71d84e9c46e29ac4a3f40892ebcea88f5a6eeb0479** (source 2e4c041c, Gowin gw_sh via tools/build.sh, 34 min, **0 setup / 0 hold**, BSRAM 131/340 unchanged — the 128-entry event log is in fabric registers; the only EX2420 latch is the pre-existing `d_lane_pix[23]` in video_stream_packer) + **fw v8 2e4c041c** (compiles: 1,041,351 B / 49 %) | not yet flashed — instrument + arm-under-hold build for the next bench session | New: (1) **bring-up event log** — GS window regs 32–39, `gs events` prints every slot and socket /RES edge, our hold edges, 0x2E and CTRL writes, por_done, clock alive/lost, storage release, MCU ready, core running/reset, trace trigger, socket pins ours/released, probe start and hardware arm with ms deltas and a context string; `gs evclear` restarts it; it stops when full so the first 128 events after a clear are kept. (2) **0x2E bit 4 ARM_EARLY** (reads back at bit 6): with AUTOARM the FPGA arms the socket while we still hold, so the core waits in reset on a driven bus like a real 65816 and starts on the machine's own /RES rise — the TransWarp model. (3) Firmware `gs mode 0..3` (0 natural = v7, **1 early = default**, 2 timed socket-off = v6, 3 timed armed) and `gs hold <ms>` for the timed modes; the `gs` line now decodes 0x2E correctly (hold = bit 4, por_done = bit 5, early = bit 6). | Bench plan: flash build 13 + fw v8 (machine off, Mac USB); power the GS with the card hands-off in mode 1; on beep or wedge run `gs events` FIRST (before anything else writes CTRL/0x2E) and paste it into the log — it answers (a) whether the core started on the machine's own rise with the pins already ours, (b) whether any /RES fall follows that is not ours (no hold/0x2E/CTRL event next to it), (c) the exact ms from clock-alive to core-running (the added-latency number against the 500 ms requirement). Then repeat cold (machine off ≥ 10 min). If mode 1 wedges, `gs mode 3` + `gs hold 300/500/1000` bisects whether the release timing or the armed-under-hold state is the variable. |
| G45 | 09-12 | **build 13 fd84b885 + fw v8 2e4c041c**, mode 1 (arm under hold), /RES-fall trigger pre-armed by hand before power-on | hands-off power-on, card USB-powered first, machine cold (off since the previous evening) | **no beep — wedged.** Event log (ms from clock-alive): 0 clock alive (hold on) → 104.4 fw writes 0x2E=1F (probe+autoarm+early), probe start, **hardware arm, socket pins OURS** (core in reset under our hold) → **283.388 SOCKET /RES rise + SLOT /RESET rise (the machine let go)** → 283.389 core RUNNING → 283.428 probe samples high: our hold OFF, POR DONE → **293.332 SLOT /RESET fall + SOCKET /RES fall + core in reset** — 9.9 ms after the core started, and the line stayed low. Nothing of ours moved at 293 ms: no hold edge, no 0x2E/CTRL write (the next writes are the firmware's 2 s make-arm-explicit at 2302 ms). GS counters: 20,258 bus cycles ran (≈2 MHz average → the ROM had already switched to fast). Pokes on the wedged machine: `gs listen` (drivers off) → slot /RESET still low; `gs off` (shifters off) → still low. Trace useless: the trigger fired on the input-enable artifact at arm time (SOCKET /RES "fall" = U13 coming on while the line is held low). | 🏆 First fully attributed wedge: **the machine itself re-asserts reset 9.9 ms after our core begins the ROM 01 cold start, and holds it until a power cycle**; none of the three card-side levers were involved (they are all in the log). The window is where ROM 01's first ADB (KEYGLU $C026/$C027) exchange sits — the same exchange the TWGS ROM performs as the very first thing after reset, with ready-polling before every byte (see the TWGS ROM row below). Also seen: the machine's own release is 283 ms after the clock in both of today's runs (M50740 RC ≈ 200 ms + its self-test). Fix for the instrument: fw `gs autotrig on` arms the trigger 20 ms after the inputs come on (945becce). The synchroniser-reset artifact (first "fall" at t=0) is cosmetic; fix the reset value next build. |
| — | 09-12 | TWGS ROM 1.8s source (`/Users/edanuff/GitHub/TransWarpGS-ROM`, ed's lead: "is the startup animation why it works?") | what the TWGS CPU does at reset release | Reset vector $FFFC → `LFB20` → `LFA9A`: (1) stack, native mode; (2) **first call = `LF94F`: poll $C027 until the KEYGLU ready bit clears (≤ 6000 tries), write SYNC $07 to $C026, then four parameter bytes ($0C, $32, $00, $00) each after polling ready (≤ 7500 tries), then a long delay loop**; (3) button-1/option pressed → skip straight to the hand-off; (4) FPGA readback check, $C02D=0, IWM motors off, NVRAM check, clear text pages, splash + timing screens (seconds), cache config; (5) emulation mode, push the GS ROM's own $00FFFC vector, RTL → ROM 01 cold start at $FA62. **It never touches GS_RESET** on the way in (Q1 is for later use). | The TWGS does not delay or re-issue reset — it starts at the machine's release exactly as we do. The difference is *what runs first*: a careful ADB SYNC with ready-polling, then seconds of its own code, so the GS ROM's cold start (with its own ADB init) runs long after the micro has settled. Hypothesis sharpened: the 9.9 ms wedge is the M50740 (or KEYGLU) reacting to how our core's cycles look during ROM 01's first $C026/$C027 exchange — or to the exchange arriving while the micro is still in its post-P25 settling. The autotrig trace on the next wedge shows the last 56 cycles before the machine pulls reset. |
| G46 | 09-12 | build 13 + fw 945becce (`gs autotrig on`), mode 1 | power cycle after G45 (machine warm: on for minutes), hands-off | **two beeps, booted**; 2.883 MHz, running, last_addr FF:CEA1, no /RES fall after the release (trigger never fired). Event log: clock alive → +109.1 probe+early arm, pins OURS under the hold → +133 autotrig CTRL D5/55 → **+283.0 machine release (socket + slot /RES rise), core RUNNING +2 µs, hold OFF/POR DONE +40 µs** → 2 s later fw makes the arm explicit. | 🏆 **Arm-under-hold boots with zero added latency**: the core starts at the machine's own release (283 ms after the clock), our hold ends 40 µs later, nothing is switched at release time — the TWGS model works on this machine. Mode 1 tally: 1 boot (warm) / 1 wedge (cold). Same sequence both times, so the variable is on the machine side (cold vs warm, i.e. the micro's state when ROM 01's first ADB exchange arrives) or in a cycle-level detail the next wedge trace will show. Next: more cycles, cold ones especially, with autotrig armed each time. |
| — | 09-12 | ed: Neil Parker, "Inside the IIGS ADB Controller" (llx.com/Neil/a2/adb.html; read in the browser pane — the site blocks curl) + MAME `apple2gs.cpp` | what the M50740 can do to the reset line | Parker: the micro's own /RESET is an RC from +5 V (reset **only at power-on**); **P2 bit 5 = the IIgs /RESET line, a plain firmware-controlled port pin** (drivable low and readable — the firmware can see our hold); P2 bit 6 = IIe-keyboard /KRESET (ROM 0/1); **Timer X = watchdog that resets the controller if it loops**; GLU status: bit 6 command-register full, bit 5 data full, bit 4 key strobe; the micro ROM can be dumped on a running IIgs (his disassembler). MAME (emulates the micro with its ROM): `adbmicro_p2_out` — P2.5 1→0 = `reset_bus()` + 65816 RESET asserted, 0→1 = released; `keyglu_mcu_read` — command-full clears only after the micro reads status then the command register. | Narrows the 9.9 ms wedge to three mechanisms on the micro: (a) its firmware reacting to the ROM's first $C026/$C027 traffic (a protocol path that drives P2.5 low and does not return), (b) the Timer X watchdog restarting it into an init that holds P2.5 low and cannot complete with the CPU now held, (c) its firmware reading P2.5 low during our hold (we hold up to 1 ms past its own release, probe period) and treating that as an external reset. The Mega II is the other candidate. The next wedge's autotrig trace shows the CPU side; the micro ROM 341-0132 (MAME romset) or a dump via Parker's tool shows the firmware side. Mode 1 boot vs wedge so far tracks cold vs warm. |
| G47 | 09-12 | build 13 + fw 945becce, mode 1, `gs autotrig on` | power cycle right after the G46 boot (machine warm), hands-off | **no beep — wedged, and this time the trace caught it.** Events (ms from clock-alive): +102.9 probe+early arm, pins OURS under the hold → +124.8 autotrig arms the /RES-fall trigger → **+282.8 machine release** (socket + slot /RES rise), core RUNNING +2 µs, hold OFF/POR DONE +40 µs → **+292.7 SLOT /RESET fall + SOCKET /RES fall, core in reset, trigger fired** — 9.919 ms after the release (G45: 9.944 ms). Nothing of ours moved. Trace (last 56 cycles): the ROM 01 clock-chip transaction loop at FF:B651–B682 — `STA $C033` (CLOCKDATA = $2C, the second byte of a BRAM extended-address command), `LDA $C034` / `AND #$3F` / `ORA #$A0` / `STA $C034` (start), then `LDA $C034` / `BMI` polling until bit 7 cleared; the last poll returned $00 and the very next cycles are the core in reset. 1.42 G cycles on the counter = the core had run ~20 k cycles again. | Two facts: (1) **the wedge is timing-locked to the machine's own release, not to our card** — 9.92/9.94 ms in both wedges, identical event sequence in the boot (G46); (2) **at 9.9 ms ROM 01 is still reading battery RAM through the VGC/clock chip and has not yet talked to the ADB micro.** ROM 03 source order (RESET → NEWRESET → SPELRESET → `LDY #$A3 / JSR GOTOBANKFF` = "setting up BATTERYRAM stuff" → … → RESETFDB = the SYNC) and the ROM 01 binary (xgs.rom on the Retro volume; its bytes at $B651 match the trace) agree. So the micro (or the Mega II) acts on its own schedule ~10 ms after releasing; the only CPU-visible traffic before that is TSB $C029, STA $C068, BIT $C010, LDA $CFFF and the clock-chip serial transactions. Mode 1 tally: 1 boot / 2 wedges. Next: characterise the machine's own reset waveform with the event log (socket off, early release) and test the timed-release-with-socket-armed path (mode 3). Note: the log's absolute ms wrap at 79.5 s (32-bit ticks); the +delta column is exact. Ed's ADB.DISASM.TXT is Neil Parker's disassembler source, not the micro ROM listing — the micro ROM itself still needs a dump (his tool on a running GS, or MAME's 341-0132). |
| G48 | 09-12 | build 13 + fw 945becce, **mode 3** (socket armed under our hold, timed release), `gs hold 1000`, autotrig on | power cycle after the G47 wedge, hands-off | **two beeps, booted**; 2.864 MHz, running, no /RES fall after the release. Events (ms from clock-alive): +108.7 CTRL 0x55 (fw arms under the hold; socket /RES input shows the held-low line; pins OURS) → +131 autotrig → **+1117.4 our release: socket + slot /RES rise, core RUNNING +1 µs** → nothing else. The machine's own release at ~283 ms happened under our hold and left no trace on the line. | The armed-under-hold state is not the variable: with the socket driven exactly as in the two wedges but the first rising edge being OURS at 1.1 s instead of the machine's at 283 ms, it boots (as v6 did 4/4 with the socket off). Combined with G45/G47: **whatever fires ~10 ms after the machine's OWN release is masked when our hold covers that moment**, and it either does not fire or does no harm when the line is already low. Timed hold 1 s is therefore a working sequence with the TWGS socket state (added latency ≈ 1 s, still above the 500 ms budget); the bisection (G39: 500 ms cold wedged once) says the masked event lasts several hundred ms. Next: the machine alone (mode 2, hold 100 ms, socket off) to log its unassisted waveform. |
| G49 | 09-12 | build 13 + fw 945becce, **mode 2, `gs hold 100`** (our hold ends 100 ms after the clock, socket OFF until the slot reset reads high), autotrig on; card repowered by the hub with the machine off (the hub does NOT keep the card up through the machine's power-down — it dropped and came back) | machine on, hands-off | **no beep — wedged.** Events (ms from clock-alive): +215.1 our hold OFF (line still low: the machine is holding it) → **+282.8 SLOT /RESET rise = the machine's own release** → +289.3 firmware sees it at the 10 ms poll, arms (pins OURS, core RUNNING +2 µs) → **+292.76 SLOT + SOCKET /RES fall, core in reset — 9.98 ms after the machine's release**, only 3.4 ms and ~3,500 cycles after the core started. Line still low minutes later. | 🏆 **The 10 ms clock runs from the machine's own release, not from the CPU start** (G45/G47: core started at +0, fall at +9.92/9.94; G49: core started at +6.5, fall at +9.98). Whatever the ROM did in 3,500 cycles cannot be the trigger, and G37 (no CPU at all) had the same outcome. So this IIgs, with the powered a2mega in the slot and its reset line released before 283 ms, releases at ~283 ms and re-asserts at ~293 ms **and never lets go**; covering the whole window with our hold (G48 1.1 s, G40 2 s, v6 4/4) boots every time. The holder must be the M50740 or the Mega II: "only a power cycle recovers" fits an M50740 parked with P2.5 low (Parker: STP = "only /RESET can resume", and its /RESET is the power-on RC). Open question — is the 283/293 double-pull native to this machine or provoked by a powered card in the slot before power-on? **Control experiment: AD3 on slot pin 31 (bus breakout) at power-on with the a2mega out of the slot** — native double pull or not. Interim: mode 3 with a 1 s hold for any card-alive-first start; slot-powered starts (card configures ~3 s after the machine, hold from power-on via the 2G06, storage-ready at config + 376 ms measured today) never expose the window and stay inside the 500 ms budget. |
| G50 | 09-12 | build 13 + fw 945becce, **mode 3, `gs hold 1000`** (armed under our hold, timed release), autotrig on, monitor on the hub | power cycle after the G49 wedge, hands-off | **boot beep, then the monitor's BRK beep** (zero boot block, G23 screen — the settings image is back in front of hdd1.po since the ESP32 restart); 2.64 MHz, running, no /RES fall after the release. Events (ms from clock-alive): +110 CTRL 0x05, pins OURS under the hold → +132 autotrig → **+1116.7 our release, socket + slot /RES rise, core RUNNING +2 µs** → nothing else. | Mode 3 tally 2/2 today (v6 timing 4/4 before): **covering the machine's 283/293 ms window with our hold and releasing at ~1.1 s boots every time, with the socket armed under the hold the whole time.** Card-first starts: this is the sequence (fw c6dbaacc makes it automatic — mode 4 picks 1 s hold when the clock appears after us, immediate release when the clock was already running at start). The "two beeps" on every good boot = chime + BRK beep from the zero boot block, not a second reset (no /RES edge in the log). |
| G51 | 09-12 | build 13 + fw 945becce, **card fully passive**: `gs off` (manual — hold released before power-on, socket shifters off, nothing armed), card hub-powered first, AD3 on slot pin 31 (ed) | machine on, hands-off | no beep (no CPU: the socket is empty and off). Events: clock alive → **+282.8 SLOT /RESET rise — and it STAYED HIGH**; no re-assert, `reset_n=1` minutes later, PHI2 1.02 MHz. | 🏆 **The 293 ms re-assert is not the machine's own habit.** With nothing of ours enabled the IIgs releases once at 283 ms and stays released. In every run that pulled at +9.9–10.0 ms after that release our socket was enabled and the core running (G45/G47 armed 180 ms before the release; G49 armed 6.5 ms after it); the only armed-at-release run that did not pull was G46 (1 of 4 → a race). So the puller (M50740 or Mega II) reacts, on a fixed ~10 ms schedule after the release, to something our enabled socket or running core does or presents in the first milliseconds — the same code a stock 65816 runs without consequence. Candidates: the lines we drive that a stock chip drives differently (VP through U15/549 Ω, the bank byte on D0–7 in PHI1, R/W, the address bus during the core's reset state), or a first-instruction cycle-shape difference the GLU/Mega II sees (BIT $C010 is within the first 20 cycles). Cheap decisive test next: **arm ≥ 20 ms after the machine's release** (fw `gs late <ms>`, mode 2) — if that boots reliably, avoiding the window is a ≤ 30 ms solution and X can be hunted at leisure with the trace. AD3 capture of this run: ed to attach (rise shape/level). |
| G52 | 09-12 | build 13 + **fw 6234a517 (mode 4 AUTO default)**, card SLOT-POWERED only (no USB, no hub — the real-user case), ribbon in | machine on, hands-off | **boot chime + BRK beep (zero boot block)** ≈ 3.5 s after power. Events (ms from FPGA configuration; the machine clock was already running at +0.15 ms — the 2G06 had held the slot reset since power-on): MCU ready 312.5 → storage-ready release 380.5 → +489.3 fw: probe + arm-under-hold (mode 4 saw the clock already up → EARLY path), pins OURS → **+490.3 first probe window finds the line free: socket + slot /RES rise, core RUNNING; hold OFF / POR DONE +40 µs** → nothing else; 2.53 MHz, running. | 🏆 **Slot-powered start works end to end and meets the budget: core running 490 ms after FPGA configuration** (312 ms ESP32 boot to MCU-ready, 68 ms to storage-ready, 109 ms clock-alive confirmation, 1 ms probe). The machine's own 283/293 ms window is long past by then, under our hold from power-on, so the re-assert never comes into play. Remaining bench item is only the card-alive-first case (dock/bench power): mode 4 covers it with a 1 s hold; the 20 ms late-arm test (`gs late`) will say whether that can drop to tens of ms. If the 109 ms alive confirmation ever matters it can shrink (10 samples at 10 ms). |
| G53 | 09-12 | build 13 + fw 6234a517 (mode 4: slot-powered path = arm under hold + immediate release), card SLOT-POWERED, AD3 DIO0 on slot pin 31 (the analog inputs read 0 V — the probe is on the digital input; `tools/ad3_reset_record.py drecord`, 500 kS/s, 2 µs resolution) | power cycle, hands-off | **no beep — wedged.** Events (ms from FPGA configuration; clock already running): storage-ready 382.7 → +493.7 probe + arm under hold, pins OURS → **+494.6 first probe window: line free, our release, core RUNNING** → **+504.6 SLOT + SOCKET /RES fall, core in reset — 9.905 ms after OUR release**; line low for good; 20,252 cycles ran. AD3: the line was LOW from the first instant of machine power through FPGA configuration (no rise at configuration — the FPGA log's 2.9 ms "high" is an input-path artifact, as ed said) until our release, then **HIGH 9.946 ms, then a hard LOW** for the rest of the record. | 🏆 The model changes: **the ~10 ms clock runs from the last rising edge of /RESET, whoever makes it** — the machine's own release (G45 9.94, G47 9.92, G49 9.98 ms) or ours seconds later (G53 9.905 ms) — **and it pulls only when our core is running when that mark arrives** (never with the socket off: G51, G37-as-reread), about half the time when the release follows soon after power/clock (G45/G47/G49/G53 pulled, G46/G52 did not) — yet in the eight 1 s-hold runs the core also started right at our release edge and none pulled, so the odds depend on how long the machine had been running under the hold before that edge; open. Whatever fires at +10 ms is on the motherboard and reacts to our running core; identity still open. Practical consequence: **do not have the core running in the first ~10 ms after any /RESET rising edge.** |
| G54 | 09-12 | **fw 974f179b — mode 4 slot-powered path = release with the socket OFF, then arm 20 ms after the line reads high** (`gs late 20`); card slot-powered only; each cycle recorded on the AD3 with the "record live" handshake | 6 power cycles, hands-off | **6/6 boots** (chime + BRK beep). Event log each time: our release 503–514 ms after configuration → SLOT /RESET rise → +32 ms socket pins OURS, core RUNNING → nothing else. AD3 each time: LOW from power-on through configuration (blips of 2 µs at ~259 ms after power in one run = the machine's own release pushing against our 2G06 hold), our release ≈ 1.64 s after machine power (configuration ≈ 1.13 s + 0.5 s), HIGH from then on, no dip at +10 ms. Also visible: the power-OFF transient is a 2.4 ms low, ~21 ms high, then low as the rails collapse. | 🏆 **Arming 20 ms after the rising edge avoids the pull: 6/6 against 4 wedges in 6 with the arm at the edge.** Card-side added latency in the slot-powered case: core running ≈ 535 ms after FPGA configuration (109 ms clock-alive confirmation + 120 ms until the release write + 20 ms late arm + 10 ms poll granularity); the 500 ms budget is met by trimming the alive confirmation (10 × 10 ms → 5 × 10 ms) and polling at 1 ms — a firmware constant, no new mechanism. Card-first (hub/bench) path still uses the 1 s cover; the same 20 ms late arm should replace it — next series. Remaining science question: what the running core does in the first 10 ms after a rising edge that a stock 65816 does not; the AD3 shows a clean hard pull, so the puller is a logic output (M50740 P2.5 or the Mega II), not contention. |
| G55 | 09-12 | fw 974f179b, **card-first (hub-powered, kept its state through the machine's power-off this time), `gs mode 2`, `gs hold 100`, `gs late 20`** | power-on, hands-off, AD3 on pin 31 | **two beeps, booted.** Events (ms from clock-alive): +218 our hold OFF (machine still holding) → **+282.8 machine's own release** → +304.3 socket armed (21.5 ms after the rise), core RUNNING → nothing else. AD3: low until the machine's release, high for the 49 s until power-off. | Card-first at **304 ms from the clock** instead of 1.1 s — when it works. |
| G56 | 09-12 | same | power cycle 2 | **no beeps — wedged, and the socket was never armed.** Events: +218 hold OFF → +282.8 machine's release → **+292.76 SLOT /RESET fall, 9.977 ms after the rise, socket OFF, no core running** (the firmware was still in its 20 ms wait). AD3: high 9.976 ms then low for good. | 🏆 **The core is not the trigger.** The pull came with our socket disabled and nothing executing. |
| G57 | 09-12 | **card fully passive** (`gs off`: hold released before power-on, socket off, manual), hub-powered | power-up 1 | no CPU so no beeps; **machine released at +282.8 ms and stayed released** (AD3: low 332 ms from the first power blip, one rise, high 42 s). | passive 2/2 clean so far (with G51). |
| G58 | 09-12 | same | power-up 2 | **machine released at +282.8 ms and pulled its own reset 9.964 ms later, permanently** — with the card doing nothing at all. | 🏆 **The 10 ms re-assert is the machine's own behaviour with the card merely present**: it happens with our hold (G45/G47/G49/G53/G56), without it (G58), with the core running (G45/G47/G53), without the core (G56/G58), socket on or off. Over all runs where the machine's own release (or an early release of ours) is the first rising edge: 6 pulls / 5 clean. When our hold covers the first second and our release is the first edge: 0 pulls / 12. **This IIgs has a marginal power-on reset**; the card's hold has been masking it all along (the Enhanced feature testing on this machine always ran under the 2G06 hold). Prime suspects on the motherboard: the M50740's own reset RC (R105 200 kΩ, C39 1 µF — an old capacitor changes the time constant and the micro's power-up), and the PSU (this machine's +5 V rail was found low and trimmed earlier). Whether the card's mere presence matters (load on the +5 V rail at power-on, bus loading) needs the control: **real 65816 in, card out, AD3 on pin 31 via the bus breakout, 6+ power-ups.** For the product: the card-first path keeps the 1 s cover (mode 4 default); the slot-powered path releases ~1.6 s after machine power, 7/8 clean (G53 the exception) — if the stock machine also pulls, the machine gets repaired (C39) and the numbers get re-taken. |
| G59 | 09-12 | **STOCK MACHINE: real 65816 in the socket, a2mega OUT of the slot**, AD3 DIO0 on slot pin 31 via the bus breakout, 90 s + 10 min records | 8 power-ups (1 + 7 inside the long record) | **8/8 clean**: power-on, low 333–337 ms (624 ms on the first, longest-off one), a single rise, high until switched off; two beeps each time (chime + the ROM's no-boot-device beep; no reset behind the second beep). Never a pull. | 🏆 **The 10 ms re-assert is not native to this IIgs — it appears only with the a2mega in the slot.** Card in, machine's own release as the first edge: 6 pulls / 11 (passive 1/3, held-then-released 5/8); card out: 0/8. The machine's own release time is the same with and without the card (~333 ms after the first power blip), so the card is not shifting the M50740's power-on RC; whatever the card does that provokes the pull happens around the release itself or in the 10 ms after it, and it needs no CPU and no socket enable (G56/G58). Next: card back in, hub-powered, passive, AD3 on /RESET plus /INH, /DMA, /IRQ, /NMI, RDY together (DIO0–5) to see which slot line moves between the release and the pull; then the same with the card slot-powered (no hub) to separate "powered before the machine" from "present". |
| G60 | 09-12 | **Ladder rung 1 (ed): a2mega in the slot, hub-powered before the machine, fully passive (`gs off`), NO ribbon, real 65816 in the socket**, AD3 DIO0 on slot pin 31 | 5 power-ups (initial + 4) | **5/5 clean**: machine release at +282.78 ms after its clock every time, line high until power-off (falls ~55 ms after the clock stops), two beeps each (chime + no-boot-device). | 🏆 **The card's presence in the slot, even powered first and passive, does not provoke the pull when a real 65816 is in the socket.** Combined with G59 (card out, real CPU: 0/8), the real CPU has now been present in 13 clean power-ups. Every pull so far happened with the ribbon in the socket instead of the chip — either our core running (~50 %) or nothing at all in the socket (2/4 passive). So the factor is the CPU socket: the motherboard re-asserts and parks, about half the time, when the ~10 ms after its reset release pass without whatever a real 65816 does — and our core, when it is running, satisfies that only sometimes. Rungs 2 (65816 out, card out) and 3 (65816 out, card in, no ribbon) will show whether an empty socket alone reproduces it on a stock machine. |
| G61 | 09-12 | **Ladder rung 2: no card in the slot, 65816 OUT, CPU socket empty**, AD3 DIO0 on slot pin 31 | 4 power-ups | **4/4 clean**: low ~335 ms (first one 622 ms, longest off), single rise, high 6–9 s until power-off. No pull. | 🏆 An empty CPU socket alone does not reproduce it: the stock motherboard with no CPU releases once and stays released. With rung 1 (card in slot, real CPU, no ribbon: 5/5 clean) and G59 (stock: 8/8), the only configurations that have ever pulled are the ones with **our ribbon in the CPU socket** — passive (2/4) or with the core running (~50 %). Whatever the ribbon/socket circuit presents to the socket pins — the card's pull-ups (VP 549 Ω to the card's +5 V, RDY), the disabled shifters, the LSF/U15 open-drain outputs — is the remaining variable. Rung 3 (card in, no ribbon, empty socket) is the last control; rung 4 (ribbon in, card passive) should reproduce; then the ribbon lines get split (lift VP, then RDY, …) or the AD3 goes onto the socket pins themselves. |
| — | 09-12 | IIgs ROM 3 netlist, CPU-socket pins by function (the KiCad symbol's pin numbers are not the DIP numbers — read the pin functions) | what the motherboard connects to the socket pins our ribbon touches | **VP → the FPI (CYA pin 25 "VP")** — the FPI watches the CPU's vector-pull output; ABORT → memory connector + 4.7 k; BE ← FPI through 100 Ω; PH2 = FPI PH2D.H; R/W → FPI R/W816 + an F86; RDY = slot RDY.H (4.7 k, FPI pin 55, UD12 74LS74); IRQ/NMI/RESB = the slot nets; **E, M/X, MLB, VDA, VPA unconnected**. | Lead for rung 4: on the card, VP is a U15 open-drain output with a **549 Ω pull-up to the card's +5 V rail**. With the card powered before the machine (hub/Mac/dock) that resistor feeds up to ~8 mA from the card's rail into the unpowered FPI's VP input — a back-feed straight into the chip that generates the CPU clock and sits on RESET.L — for the whole time the machine is off, and it holds VP at 5 V through the FPI's power-up. With an empty socket (rung 2) VP floats; with a real 65816 it is the chip's output. Every configuration that has pulled had the ribbon (and this pull-up) on the socket, and the powered-first cases pull most; the slot-powered cases (card rail = slot rail, no back-feed before power) were 6/6 clean with the late arm, 1 pull in 8 overall. Test when we return to the socket: ribbon in, card passive, hub-powered — first with VP (IDC pin 1) isolated or the 549 Ω lifted, then with it connected. If VP isolation makes it clean, the fix is on the card (the pull-up rail or a series element on VP), not in the sequence. |
| G62 | 09-12 | **Ladder rung 3: card in the slot, hub-powered before the machine, passive (`gs off`), NO ribbon, CPU socket EMPTY** | 5 power-ups (initial + 4) | **3 pulls / 5**: releases at +282.78 ms every time; pulls at **+9.958, +9.949, +9.951 ms** after the release on cycles 2, 4 and 5; cycles 1 and 3 stayed high until power-off. | 🏆 Reproduced **without the ribbon**, so the VP pull-up and everything else on the socket side are cleared. The complete ladder: real 65816 in the socket → 13/13 clean (card out or in); empty socket, no card → 4/4 clean; empty socket, card in (passive, powered first) → 3/5 pulls; our core in the socket → ~50 %. **The pull needs the a2mega in the slot AND the absence of a real CPU executing.** The timer is the motherboard's (9.95 ms ± 10 µs after its own release); what the card contributes is either electrical (a hub-powered card back-feeding the unpowered motherboard through the slot pull-ups before power-on — the slot-powered late-arm series was 6/6 clean) or its emulated slot devices responding to a CPU-less bus. Next, software-only: quiet the emulated cards (empty slot map) and repeat; then, if still pulling, the same with the hub unplugged while the machine is off (card dark before power) once the sequence allows it. |
| G63 | 09-12 | Rung 3b: as G62 but **every emulated slot card set to EMPTY at runtime** (regs 0x30/0x31 = 0 for slots 0–7, 0x33 reconfig; slot status 0x32 = 0) — card in, no ribbon, empty socket, passive, hub-powered first | 5 power-ups | **2 pulls / 5** (+9.973, +9.976 ms after the release), 3 clean. AD3 (one 10-minute record spanning rungs 3 and 3b): every pull is a 9.95–9.98 ms high after the 333 ms low, then low until power-off; clean cycles are a single rise — identical to the FPGA log in all ten power-ups. | The emulated devices are not it. Summary of everything with the machine's OWN release as the first rising edge: no card → 0/12 (stock 8, empty socket 4); card present, any state, no real CPU → 10 pulls / 19; card present, real CPU → 0/5. With OUR release as the first edge ≥ 1.1 s after the clock → 0/12; at ~1.6 s after power (slot-powered) → 1/8. The card's electrical presence on the slot, while the machine's own release at 283 ms is the first edge and no real CPU is executing, is the condition; the ~10 ms timer is the motherboard's. Next instrument, no board handling: AD3 DIO1–5 on the bus breakout = /INH (32), /DMA (22), /IRQ (30), /NMI (29), RDY (21) alongside /RESET, passive series — which slot line moves in the 10 ms names the mechanism; in gateware, log the card's own slot-output states as events. |
| G64 | 09-12 | Card in the slot, hub-powered first, passive (`gs off`; the card browns out when the machine goes off and comes back on defaults — re-made passive before the series), NO ribbon, CPU socket EMPTY, **IIe keyboard UNPLUGGED from J13** (its /KRESET line has a +5 V pull-up on the board, so the micro's Control-Reset condition cannot be met) | 10 power-ups (4 + 6) | **10/10 clean**: release at +282.78 ms every time, high until power-off, our hold released on all ten (40 log events = 10 × alive/rise/lost/power-off fall, no fall while the clock was alive). | Same electrical configuration as G62/G63 (6 pulls in 10 there) with only the IIe keyboard removed → 0 in 10 (p ≈ 0.2 % by chance at the earlier rate). **The IIe keyboard on J13 is required for the pull.** Plug-back-in series next to close it. If it holds, the puller is the M50740 via its IIe-keyboard Control-Reset path (`$146C`: /KRESET low → `$1475` → CNTRL low / `$03.7` / `$0B.0` → assert `$147E`, held while /KRESET reads low), which would also explain "keyboard plugged in + card present + no real CPU": something on this board's J13 lines reads low at the micro ~10 ms after the Mega II release under those conditions. Notes: adb_micro_rom01_notes.md. |
| G65 | 09-12 | As G64 but **IIe keyboard plugged back into J13** (card passive, hub-powered first, no ribbon, empty socket; hold released on every cycle) | 6 power-ups | **2 pulls / 6** (+9.963 and +9.978 ms after the release, permanent), 4 clean. AD3 (one 10-minute record spanning all ten keyboard-out cycles and the first plugged-back-in one): ten single rises with no dip, then — first cycle with the keyboard back — a 9.964 ms high and low for good, pull for pull with the FPGA log. | 🏆 Keyboard in: 8 pulls / 16 across G62/G63/G65; keyboard out: 0 / 10 (G64). **The IIe keyboard on J13 is a necessary part of the condition**, together with the a2mega in the slot and no real 65816 executing. The only element of the reset topology that reads J13 is the M50740's Control-Reset path (`$146C` → /KRESET low → `$1475` → CNTRL low / `$03.7` / `$0B.0` → assert `$147E`, held while /KRESET reads low), so the puller is the micro, asserting because its keyboard-connector inputs read "Control-Reset" ~10 ms after the Mega II releases. Why those lines read low then — and why a real CPU's presence prevents it while our core's does not — is the remaining question; J13 and ADB cannot be probed on this bench, so the next discriminators are with our core in the socket: keyboard out vs in (does the product configuration show the same dependence), and the micro's view of the GLU (spurious command traffic from a CPU-less bus with the card present). Practical position unchanged: the firmware's hold through the machine's first second (card-first) and the 0.5 s-after-configuration release (slot-powered) avoid the window. |
| G66 | 09-12 | As G65 (card passive, hub-powered first, no ribbon, empty socket, IIe keyboard in) but **AD3 disconnected and the bus breakout removed** — FPGA event log only | 6 power-ups | **4 pulls / 6** (+9.955, +9.969, +9.969, +9.973 ms), 2 clean; hold released on all six. | The scope and breakout are not part of the condition (G45/G47/G49 had already pulled before the AD3 was ever attached). Running tally with the card present, socket empty, IIe keyboard in: 12 pulls / 22; keyboard out: 0 / 10. ROM 01 J13 schematic (ed): **KRESET.L → micro P26 only, R125 4.7 k to +5 V** (CNTRL → P31 R126, CAPLOCK → P32 R127, SHIFT → P30, KSW0/1 → P36/37, X0–7 → P10–17); the keyboard's Y rows are driven by the ADB GLU's SC0–SC9, and the GLU is on RESET.L with its CPU-side pins (KSEL0–2, PH0, R/W, D0–7) on the bus. Next: the same passive series on the ROM 03 machine (TWGS out, socket empty, ADB keyboard, M50741) — pulls there would rule the IIe-keyboard path out; none would support it. |
| G67 | 09-12 | **ROM 03 machine** (TWGS card and its ribbon removed → CPU socket empty; ADB keyboard; M50741), a2mega in a slot, USB/hub-powered first, passive (`gs off`), no ribbon, FPGA log only | 7 power-ups | **7/7 clean**: release at +282.776 ms every time (same Mega II power-on timing as the ROM 01's 282.780), high until power-off (the line falls ~230 ms after the clock stops on this machine, vs ~55 ms on the ROM 01 — different supply decay), hold released on all seven. | 🏆 **The re-assert does not occur on the ROM 03.** Same card, same passive state, same empty socket; the differences are the machine: ROM 03 has no IIe keyboard path (those M50741 pins are grounded), a different micro firmware, and an ADB keyboard. With the ROM 01 results — 12 pulls / 22 with the IIe keyboard on J13, 0 / 10 with it unplugged — the condition is now: **ROM 01 + IIe keyboard on J13 + a2mega in the slot + no real 65816 executing**, and the mechanism is the M50740's IIe-keyboard Control-Reset path (firmware `$146C`/`$147E`, KRESET.L → P26 with a 4.7 k pull-up). Why P26 reads low ~10 ms after the Mega II release only under those conditions remains open (keyboard/cable coupling from the GLU-driven Y rows, or a firmware route not yet traced); the card-side mitigation — never let the machine's own release be the first edge — is unchanged and now known to be needed only for this class of machine. |
| G68 | 09-12 | **ROM 01, ADB keyboard (from the ROM 03) on J18, IIe keyboard NOT connected to J13**, card in the slot, hub-powered first, passive, no ribbon, empty socket | 6 power-ups | **6/6 clean**: release at +282.78 ms, high until power-off, hold released on all six. | With an ADB keyboard answering the micro's polls (so the ADB reset-key flag path `$14FF`/`$1524` is live) and J13 empty, no pull. Together with G64 (J13 empty, no ADB keyboard: 0/10) and the keyboard-in runs (12/22): **the IIe keyboard on J13 is the specific requirement**, not "a keyboard" — the micro's ADB reset-key logic does not misfire under the card-present, no-CPU condition, only its J13 /KRESET input does. Final tally, all runs with the card in the slot and no real 65816: IIe keyboard on J13 → 12 pulls / 22; J13 empty (with or without an ADB keyboard) → 0 / 16; ROM 03 → 0 / 7. Remaining question: why KRESET.L (J13 pin 15 → micro P26 through R125 4.7 k) reads low at the micro ~10 ms after the Mega II release under that condition — the keyboard/cable side (rows driven by the GLU) is the place to look, and it is a ROM 01-with-IIe-keyboard property, not a card defect. Card-side mitigation unchanged and sufficient. |
